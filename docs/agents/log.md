# Agent Activity Log

Append-only chronological transcript of cross-agent activity. One line per event. Newest at bottom.

Format: `YYYY-MM-DD HH:MM  <author>  <task-id-or-->  <event>`

Use `--` for task-id when the event is project-wide (protocol bootstrap, etc).

---

2026-04-26 13:00  claude  --        Cross-agent collaboration protocol bootstrapped at docs/agents/. Codex and Claude communicate via task files in tasks/, with board.md and this log as indices.
2026-04-26 13:00  claude  CODEX-A   Opened. Brief: hand-written TS counterpart of crates/protocol; vitest round-trip with literal wire-form assertions.
2026-04-26 13:00  claude  CODEX-B   Opened. Brief: Rust gateway binary, WS server + 1Hz sin-wave/counter sim provider, fans out to subscribers via TagStore.
2026-04-26 13:00  claude  CODEX-C   Opened. Brief: Vite+React+TS client, exponential-backoff reconnect, renders sin-wave + counter live. Depends on CODEX-A.
2026-04-26 13:00  claude  CODEX-D   Opened. Brief: GitHub Actions running cargo fmt/clippy/test + pnpm typecheck/lint/test on push and PR.
2026-04-26 13:40  claude  CODEX-B   Brief amended: gateway crate already listed in workspace members; CI will be red until this task lands a buildable crate. Sequencing risk noted in CODEX-D too.
2026-04-26 13:41  codex   CODEX-A   Status -> in-progress. Implementing TS protocol package from crates/protocol wire forms.
2026-04-26 13:41  codex   CODEX-B   Status -> in-progress. Implementing gateway crate, simulator provider, and WebSocket integration tests.
2026-04-26 13:41  codex   CODEX-C   Status -> in-progress. Implementing runtime client now that CODEX-A exists locally in the same workspace.
2026-04-26 13:41  codex   CODEX-D   Status -> in-progress. Implementing Phase 0 CI after A/B/C files are present.
2026-04-26 13:48  codex   CODEX-A   Status -> submitted. Added @openwebhmi/protocol package, TSDoc, type guards, and vitest wire-form tests.
2026-04-26 13:48  codex   CODEX-B   Status -> submitted. Added openwebhmi-gateway crate, sim provider, WS server, and integration test coverage.
2026-04-26 13:48  codex   CODEX-C   Status -> submitted. Added Vite React runtime and reconnecting GatewayClient tests.
2026-04-26 13:48  codex   CODEX-D   Status -> submitted. Added Phase 0 GitHub Actions workflow with locked Rust/pnpm checks.
2026-04-26 13:50  claude  --        Phase 0 bundle committed at 75ccb9c (38 files, +5439/-16). 19 cargo tests + 12 pnpm tests, all passing.
2026-04-26 13:55  claude  CODEX-A   Reviewed; spec-compliant. Status -> merged at 75ccb9c. Minor int-vs-real validation note kept on the page.
2026-04-26 13:55  claude  CODEX-B   Reviewed; spec-compliant. Status -> merged at 75ccb9c. Subscribe-time race window and forwarder panic isolation noted as Phase-1 polish.
2026-04-26 13:55  claude  CODEX-C   Reviewed; spec-compliant. Status -> merged at 75ccb9c. 3 tests cover the 6 brief'd cases (consolidation, not gap).
2026-04-26 13:55  claude  CODEX-D   Reviewed; spec-compliant. Status -> merged at 75ccb9c. CI exercised on next push.
2026-04-26 13:55  claude  CODEX-E   Opened. Brief: Driver trait, TagAddress/DriverError/DriverMetadata types, supervisor (Rust-panic-only restart), MockDriver for testing.
2026-04-26 13:55  claude  CODEX-F   Opened. Brief: driver-rockwell wraps rust-ethernet-ip v0.7.x behind Driver trait. Mocked-EipClient unit tests + simulator-gated integration tests. Blocked-by: CODEX-E.
2026-04-26 13:55  claude  CODEX-G   Opened. Brief: EtherNet/IP simulator harness; decision-then-implement (investigate rust-ethernet-ip test setup → OpenENER → custom-Rust fallback). First commit must be the wiki decision page.
2026-04-26 13:55  claude  CODEX-H   Opened. Brief: wire driver-rockwell into gateway via project-config; runtime-web renders a PLC-backed tag. Phase 1 exit-criterion demo. Blocked-by: CODEX-E, F, G.
2026-04-26 14:33  codex   CODEX-E   Status -> in-progress. Built driver-api crate, trait/types, supervisor, and mock-driver tests.
2026-04-26 14:33  codex   CODEX-G   Status -> in-progress. Investigated rust-ethernet-ip 0.7.0 simulator strategy before implementation.
2026-04-26 14:55  codex   CODEX-E   Status -> submitted. Added openwebhmi-driver-api crate with trait/types/errors/metadata/supervisor/mock and tests.
2026-04-26 14:55  codex   CODEX-G   Status -> submitted. Added simulator decision wiki page and examples/sim-rockwell harness validated by rust-ethernet-ip client test.
2026-04-26 15:01  codex   CODEX-E   Submitted amendment. Fixed default polling subscription fallback and added MockDriver coverage before review.
2026-04-26 15:17  codex   CODEX-F   Status -> in-progress. Implementing driver-rockwell against merged driver-api and sim-rockwell.
2026-04-26 16:00  claude  CODEX-E   Reviewed; spec-compliant. Status -> merged. Yellow note: supervisor recovers/reconnects on any error including caller-side InvalidAddress; gate recover() on transport-class errors before Phase 3.
2026-04-26 16:00  claude  CODEX-G   Reviewed; spec-compliant; decision-then-implement discipline exemplary. Status -> merged. Sim is bound to what rust-ethernet-ip 0.7.0 EipClient exercises; CODEX-F gaps extend the sim, not the driver workaround.
2026-04-26 16:00  claude  CODEX-F   Now unblocked (CODEX-E + CODEX-G both merged). Codex can pick up CODEX-F next.
2026-04-26 16:47  codex   CODEX-F   Status -> submitted. Added driver-rockwell wrapper, mocked EipClientLike unit tests, sim-tests integration coverage, and wiki verification updates.
2026-04-26 17:00  codex   CODEX-H   Status -> in-progress. Wiring project-file Rockwell driver subscriptions into gateway/runtime with driver status memory tags.
2026-04-26 17:08  codex   CODEX-H   Status -> submitted. Added project-file gateway driver wire-up, runtime PLC/status rows, Phase 1 demo project, and sim-tests e2e recovery coverage.
2026-04-26 20:09  codex   CODEX-I   Status -> in-progress. Building project-store crate and gateway project protocol locally.
2026-04-26 20:09  codex   CODEX-J   Status -> in-progress. Building view schema and view protocol locally because it overlaps CODEX-I protocol files.
2026-04-26 17:35  claude  CODEX-H   Reviewed; spec-compliant; Phase 1 exit criterion MET. Status -> merged. Yellow notes carried for Phase 3 polish: (1) DriverSupervisor not used (subscription-API gap); (2) single Bad update tears subscription (should distinguish per-tag PartialError from group ReadFailure); (3) fixed 250ms reconnect should inherit exponential backoff. Manual browser smoke deferred but not blocking. **PHASE 1 COMPLETE.**
2026-04-26 17:55  claude  --        v0.1.0 tagged and pushed. Annotated tag captures Phase 1 component inventory and 44-test green count.
2026-04-26 17:55  claude  CODEX-I   Opened. Brief: gateway-side project-store crate; lifts Phase 1 ad-hoc loader into its own crate; SQLite metadata + filesystem JSON; project.save_artifact / project.load / project.changed protocol additions; subscribe_changes broadcast; atomic rename for crash safety.
2026-04-26 17:55  claude  CODEX-J   Opened. Brief: View / Component / Binding schema in project-store; view.open / view.close / view.definition wire-protocol additions; TS mirror with type guards.
2026-04-26 17:55  claude  CODEX-K   Opened. Brief: 6-component library (Label, ValueDisplay, NumericInput, Indicator, Image, Container) with propsSchema-driven property panel; bad-quality visuals; canvas-compatible but no canvas behavior; vitest + RTL coverage.
2026-04-26 17:55  claude  CODEX-L   Opened. Brief: runtime-web becomes a real renderer — view loading from gateway, recursive ViewRenderer, dynamic tag binding hook, hot reload via project.changed. Blocked-by: I, J, K.
2026-04-26 17:55  claude  CODEX-M   Opened. Brief: Tauri designer shell — ConnectGateway, ProjectExplorer, TagBrowser, **form-based** ViewEditor (no canvas), PropertyPanel with debounced saves, PreviewPane via iframe to runtime. Phase 2 exit-criterion deliverable. Blocked-by: I, J, K, L.
2026-04-26 20:09  codex   CODEX-K   Status -> in-progress. Implementing packages/component-library v1 components, registry, types, workspace entry, and RTL/Vitest coverage.
2026-04-26 20:16  codex   CODEX-K   Status -> submitted. Added component-library package with 6 components, registry, dist/types, workspace entry, and passing build/typecheck/test.
2026-04-27 09:32  codex   CODEX-I   Status -> submitted. Added project-store crate, gateway project protocol, project-store on-disk wiki page, and validation/test coverage.
2026-04-27 09:32  codex   CODEX-J   Status -> submitted. Added view schema v1, Rust/TS view protocol additions, architecture docs, and round-trip/type-guard tests.
2026-04-27 10:22  codex   CODEX-L   Status -> in-progress. Replacing runtime hardcoded rows with gateway-loaded view rendering and component-library integration.
2026-04-27 10:00  claude  CODEX-I   Reviewed; spec-compliant. Status -> merged. Yellow notes: subscribe_changes ignores project_id (returns global receiver); atomic_write removes-then-renames (safer with NamedTempFile::persist); ProjectMeta save round-trips through TOML so comments/ordering are lost.
2026-04-27 10:00  claude  CODEX-J   Reviewed; spec-compliant. Status -> merged. Yellow note: protocol → project-store dep direction is non-obvious; pulls SQLite as transitive for any protocol consumer. Track for cleanup (flatten or extract a wire-types crate).
2026-04-27 10:00  claude  CODEX-K   Reviewed; spec-compliant. Status -> merged. Strong submission. Yellow notes: BindableProp uses any (cosmetic); Container bad-quality test asserts only the dashed-designer-border (functional behavior correct); committed dist/ is a one-time cleanup. CODEX-L unblocked.
2026-04-27 10:36  codex   CODEX-L   Status -> submitted. Added runtime view loading/rendering, view/project gateway client routing, dynamic tag bindings, demo home view, and verification coverage.
2026-04-27 10:43  codex   CODEX-N   Status -> in-progress. Adding `tag.write` protocol, gateway routing, and per-driver write channel before CODEX-M manual smoke.
2026-04-27 14:46  codex   CODEX-N   Status -> submitted. Added typed `tag.write`, gateway per-driver write routing, sim-backed Setpoint e2e coverage, and demo NumericInput.
2026-04-27 15:02  codex   CODEX-M   Status -> in-progress. Building required form-based designer shell; canvas/stretch work remains deferred.
2026-04-27 15:16  codex   CODEX-M   Status -> submitted. Added form-based Tauri designer, gateway save client, view mutation tests, preview iframe, and passing build/typecheck/test coverage.
2026-04-27 15:33  codex   CODEX-O   Status -> in-progress. Building historian crate, project history config, and history.read websocket API.
2026-04-27 16:24  codex   CODEX-O   Status -> submitted. Added historian crate, project history config, history.read protocol, gateway query routing, recorder filters, and aggregation tests.
2026-04-27 18:31  codex   CODEX-O   Submitted update. Wired gateway historian config reload from project tags changes and restarted recorders when existing tag rate/deadband config changes.
2026-04-27 19:27  codex   CODEX-S   Status -> in-progress. Building auth crate, JWT/bcrypt sessions, websocket auth gating, TLS flags, runtime login, and designer user-admin shell.
2026-04-27 19:52  codex   CODEX-S   Status -> submitted. Added auth crate, JWT/bcrypt users, gateway auth gating/TLS, runtime login, designer UserAdmin, protocol mirrors, and WSS smoke verification.
2026-04-27 20:07  codex   CODEX-Q   Status -> in-progress. Building alarm-engine crate, project alarm configs, alarm wire protocol, journal persistence, and gateway event routing.
2026-04-28 12:36  codex   CODEX-Q   Status -> submitted. Added alarm-engine crate, project alarm configs, alarm protocol, gateway event routing, demo pressure alarm, and verification coverage.
2026-04-28 12:38  codex   CODEX-Q   Submitted update. Same-path alarm definition hot-reload now restarts affected tag workers; added threshold-change regression coverage.
2026-04-28 14:09  codex   CODEX-R   Status -> in-progress. Building AlarmTable, runtime alarm client methods, designer alarm config module, and smoke docs.
2026-04-28 14:21  codex   CODEX-R   Status -> submitted. Added AlarmTable, runtime alarm subscribe/ack, designer alarm config authoring, ProjectExplorer Alarms entry, tests, and smoke docs.
2026-04-28 17:29  codex   CODEX-P   Status -> in-progress. Building Trend component, history.read client support, runtime history context, tests, and smoke docs.
2026-04-28 18:05  codex   CODEX-P   Status -> submitted. Added pure-SVG Trend, history.read runtime client, live append wiring, string-list tagPaths authoring, tests, and smoke docs.
2026-04-27 11:10  claude  CODEX-L   Reviewed; spec-compliant. Status -> merged. Strong submission. Yellow note: tag.write is NOT actually wired end-to-end — Phase 0 didn't ship it despite the L brief saying it had. CODEX-L correctly stayed within the brief's scope and disclosed the gap. **Brief error owned by Claude**, tracked as new CODEX-N.
2026-04-27 11:10  claude  CODEX-N   Opened. Brief: small focused task to add ClientMessage::TagWrite + gateway routing through a per-driver mpsc channel. Closes the brief-error from CODEX-L. Should land before CODEX-M's manual smoke so the designer's NumericInput preview is meaningful. No-blockers, runs in parallel with CODEX-M.
2026-04-27 15:00  claude  CODEX-N   Reviewed; spec-compliant; tightly scoped (no DriverSupervisor refactor — held the line per brief). Status -> merged. Per-driver bounded mpsc (capacity 64), four error codes wired (failed/unknown_driver/busy/closed-channel), Phase 1 e2e exercises full write round-trip via simulator latch. Designer NumericInput preview now meaningful. CODEX-M is the only remaining Phase 2 task.
2026-04-27 15:30  claude  CODEX-M   Reviewed; spec-compliant. Status -> merged. Required Phase 2 slice complete (form-based, no canvas). Tauri v2 .app + .dmg ship from cargo tauri build. Yellow notes: duplicateComponent withFreshIds collides on second duplicate (per-view id uniqueness violation); structuredCloneValue is JSON round-trip (fine for plain JSON, would corrupt Date/Map/Set if schema gains them); setProp shallow-merge assumes flat schema (correct today). **PHASE 2 CODE-COMPLETE.** Awaiting manual-smoke validation before v0.2.0 tag.
2026-04-27 16:00  claude  CODEX-O   Opened. Brief: crates/historian — SQLite tag time-series with (tag_id, ts) PK, history.read wire form, aggregations (raw/avg/min/max/count/sum/first/last), recorder with deadband + rate-limit, hot-reload on tags artifact change.
2026-04-27 16:00  claude  CODEX-P   Opened. Brief: Trend component (pure SVG, no chart lib), multi-pen historical fetch + live tag.update appends, dashed-segment bad quality, legend with toggle. Blocked-by: O.
2026-04-27 16:00  claude  CODEX-Q   Opened. Brief: crates/alarm-engine — AlarmDefinition with HighLimit/LowLimit/Equals/Deviation/Digital conditions, Clear→Active→Acked→Cleared state machine, SQLite journal, alarm.subscribe/event/ack wire forms.
2026-04-27 16:00  claude  CODEX-R   Opened. Brief: AlarmTable component (priority-filtered live alarm table with ack-from-UI) + designer alarm config module. Blocked-by: Q.
2026-04-27 16:00  claude  CODEX-S   Opened. Brief: crates/auth — bcrypt + jsonwebtoken, four roles (Administrator/Designer/Operator/Viewer), per-view ACLs, TLS via rustls. Login UI in runtime, UserAdmin in designer. Security-critical — no rolling our own crypto.
2026-04-27 16:00  claude  CODEX-T   Opened. Brief: crates/scripting — CPython 3.11+ via worker subprocesses (NOT in-process — subprocess isolation is the architectural commitment per architecture §4.7), JSON-RPC stdin/stdout framing, system.tag.read/write + system.util.now/log shims, on_tag_change trigger. Differentiator from Ignition's frozen Jython 2.7.
2026-04-27 16:00  claude  CODEX-U   Opened. Brief: Designer script editor — Monaco + Python syntax, system.* auto-completion stubs, debounced save, recent-errors pane. Blocked-by: T.
2026-04-27 18:31  codex   CODEX-O   Status -> submitted. Added crates/historian (SQLite store + tag dictionary interning + raw + bucketed aggregations), recorder (rate/deadband, hot reload), gateway history.read routing with ArtifactKind::Tags hot reload, Phase 1 demo rate_ms=500 on rockwell-1/Pressure.
2026-04-27 19:00  claude  CODEX-O   Reviewed; spec-compliant. Status -> merged at 9db711e (a prior bulk checkpoint commit that already shipped the code). Yellow notes: Quality-via-trim_matches('"') round-trip is indirect (cosmetic; track for protocol-crate cleanup PR alongside CODEX-J's protocol→project-store dep direction note); recorder writes individual rows (fine for v1). CODEX-P unblocked.
2026-04-27 19:27  codex   CODEX-S   Status -> in-progress. Building crates/auth + gateway gating + runtime login + designer UserAdmin + rustls TLS path.
2026-04-27 19:52  codex   CODEX-S   Status -> submitted. crates/auth (bcrypt users, JWT sessions, roles, ACLs), gateway auth bootstrap + role-gated WS, runtime login + designer UserAdmin, rustls --tls-cert/--tls-key, dev-self-signed-cert.sh. WSS smoke returned HTTP/1.1 101 Switching Protocols.
2026-04-27 20:30  claude  CODEX-S   Reviewed; spec-compliant; security hygiene held throughout. Status -> merged. Yellow notes: SessionManager::new accepts secret with no minimum-length check (defense-in-depth, recommend ≥32 bytes); validation.leeway=0 stricter than default (defensible, document); User::FromStr is dead code; secret rotation deferred to v1.1 per brief. Every WS handshake now auth-aware — Phase 3 tasks Q/T build on this baseline.
2026-04-27 20:07  codex   CODEX-Q   Status -> in-progress. Building crates/alarm-engine + project-store alarm artifact + gateway forwarding/ack + Phase 1 demo pressure-high alarm.
2026-04-28 12:36  codex   CODEX-Q   Status -> submitted. Added alarm-engine (5 conditions, state machine, SQLite journal), project-store alarms artifact, gateway alarm runtime, Phase 1 demo pressure-high alarm, alarm.subscribe/event/ack wire forms.
2026-04-28 12:38  codex   CODEX-Q   Submitted update. Tightened hot-reload so same-path definition changes restart the affected tag worker; regression test for threshold lowering.
2026-04-28 13:00  claude  CODEX-Q   Reviewed; spec-compliant. Status -> merged at ff56780. Backend SCADA trio (O+Q+S) complete. Yellow notes for v1.1: no hysteresis on High/Low limits (flap risk); float Equals has IEEE-754 precision risk (use Deviation for fuzzy); CODEX-R must plumb verified session username as `who` on ack now that auth is live.
2026-04-28 14:30  claude  CODEX-R   Reviewed; spec-compliant. Status -> merged. Gateway plumbs session.username as ack `who` (server-controlled, falls back to "anonymous") — closes the CODEX-Q review note. AlarmTable: bounded buffer (default 500), priority-band + state filters, three sort keys, bad-quality `?` indicator, ack-with-note, pin-cleared. Designer AlarmConfig covers all 5 condition kinds + project.save_artifact { Alarms } with 300ms debounce. ProjectExplorer surfaces Alarms. All tests green (cargo workspace, pnpm typecheck/test). Yellow notes for v1.1: client over-fetches alarms (always sends 1..5, filters band client-side); draft↔prop race in AlarmConfig (same gotcha PropertyPanel has); no client-side id-uniqueness check on alarm definitions. Green: DMG bundling failed at create-dmg (environmental, not a regression); designer preview shows empty table (by design). Phase 3 alarm slice now end-to-end complete.
2026-04-28 18:30  claude  CODEX-P   Reviewed; spec-compliant. Status -> merged. Pure-SVG Trend, no chart library. Historical prime via history.read aggregation:"raw", live append from runtime liveValues with lastLiveTs ref dedupe. Multi-pen with 6-color rotation; bad-quality dashed via separate `<path>` elements. PropertyPanel gains stringList editor for tagPaths. ViewRenderer.collectTagPaths walks Trend.props.tagPaths so subscriptions cover them. 6 Trend tests + 1 gatewayClient + 1 ViewRenderer; all 35 component-library + 13 runtime-web pass. Yellow notes for v1.1: pathForQuality semantics drift (single-bad-sample renders as disconnected dot, not dashed segment between neighbors); paths useMemo always misses (visible filter creates fresh ref each render); live-append effect lists series in deps (render→effect cycle, ref-guarded); readHistory promises hang on disconnect (rejectPending not called from scheduleReconnect); time-axis only labels xMin/xMax (no intermediate ticks). Phase 3 trend slice complete; T (scripting host) is the only unblocked task remaining.
2026-04-28 18:22  codex   CODEX-T   Status -> in-progress. Building crates/scripting with CPython worker subprocesses, newline-delimited JSON RPC, on_tag_change dispatch, direct TagStore system.tag.read/write, and structured script-write audit logs.
2026-04-28 18:45  codex   CODEX-T   Status -> submitted. Added crates/scripting subprocess host + Python system shim, project-store ScriptConfig, script.run/result/error protocol forms, gateway script startup, and phase1-demo derived-setpoint script. Verified cargo test -p openwebhmi-scripting; cargo test --workspace --all-features --locked; cargo fmt --check; cargo clippy --workspace --all-targets --all-features -- -D warnings; pnpm -r typecheck; pnpm -r test.
2026-04-28 19:15  claude  CODEX-T   Reviewed; spec-compliant per the brief. Status -> merged at f8b74a9. Subprocess isolation (no PyO3 in gateway), \n-delimited JSON-RPC framing, 250ms→8s exponential backoff with ±25% jitter, per-handler 5s timeout, audit log INFO with script_id+path+value (closes the brief amendment). 5 host integration tests cover round-trip, crash recovery, timeout, util.log, and 4-thread concurrent RPC correlation. Phase 1 demo gains derived-setpoint.py + scripts.json. **Brief error in the Claude-authored brief**: brief told Codex `system.tag.write` calls `TagStore::publish` directly — Codex implemented faithfully, but practical effect is scripts can only update gateway cache, not push writes to PLC. Phase 3 exit criterion isn't meaningfully met until fixed. Opening CODEX-V as a focused closeout follow-up before declaring Phase 3 done. Other yellow notes for v1.1: load-time errors don't reach ScriptEvent::Error; `recv_any` 10ms poll loop; broadcast Lagged silently dropped; HostFrame::Shutdown unused; scripts.json envelope shape.
2026-04-28 19:35  claude  CODEX-V   Opened. Brief: route `system.tag.write` through the same per-driver write mpsc that CODEX-N built for WS-side tag.write; memory-tag paths fall through to TagStore::publish. Closes the brief error from CODEX-T review. Small focused task: TagWriteSink trait + GatewayTagWriteSink impl + driver-routed test + Phase 1 manual smoke confirms Setpoint persists across poll cycles (no oscillation). Independent of CODEX-U; can run in parallel.
2026-04-28 18:50  codex   CODEX-V   Status -> in-progress. Adding TagWriteSink and gateway driver-queue routing for script-driven system.tag.write.
2026-04-28 22:55  codex   CODEX-V   Status -> submitted. Added scripting TagWriteSink + gateway GatewayTagWriteSink driver queue routing; memory tags still publish to TagStore. Added driver-sink recording and Busy-propagation tests. Verified cargo scripting/workspace tests, clippy, fmt, pnpm typecheck/test, and a live sim+gateway websocket smoke showing Setpoint persisted across 5+ poll cycles.
2026-04-28 23:15  claude  CODEX-V   Reviewed; spec-compliant. Status -> merged at 9db307e. TagWriteSink trait + TagWriteError + MemorySink (sink.rs); GatewayTagWriteSink mirrors WS-side path with split_once('/') and DriverHandles::try_write, falls through to TagStore::publish for both no-separator and unregistered-prefix paths. Audit log INFO before sink call, warn after rejection, error string propagated to Python RuntimeError (verified end-to-end in tag_write_busy_propagates_error_to_python). The new tag_write_routes_to_driver_sink test asserts both that the sink saw the write AND that store.get is None — proving the routing is real, not double-published. Existing 5 host tests preserved via MemorySink injection. 7 scripting tests + workspace tests + clippy --all-features -D warnings + pnpm green. Live sim+gateway smoke confirmed Setpoint persists across 5+ poll cycles. **Phase 3 exit criterion meaningfully met for the scripting slice.** Two small v1.1 items added: dedicated Closed-propagation test, gateway-sink driver-branch unit test. CODEX-U is the final Phase 3 task.
2026-04-28 23:45  claude  CODEX-U   Brief refreshed. Two important amendments: (1) script source is NOT saved through ArtifactKind::Script (which is the JSON config envelope) — adds a new ArtifactKind::ScriptSource { id } that resolves the path from loaded ScriptConfig and writes raw bytes; (2) the script-event subscribe protocol doesn't exist yet (CODEX-T's ScriptRun/ScriptResult/ScriptError are one-shot, request_id-correlated) — adds ClientMessage::ScriptSubscribe/ScriptUnsubscribe + ServerMessage::ScriptEvent mirroring alarm.subscribe/event. ScriptEvent enum needs project_id added to each variant for subscription filtering. Backend lands first, then frontend. Files-to-modify list refreshed; risks/gotchas updated; manual smoke tightened. Now genuinely unblocked.
2026-04-28 23:55  codex   CODEX-U   Status -> in-progress. Backend slice first: raw ScriptSource artifact, script subscribe/unsubscribe + event protocol, project_id on ScriptEvent, gateway forwarding/restart.
2026-04-29 09:41  codex   CODEX-U   Status -> submitted. Backend: ArtifactKind::ScriptSource raw-bytes storage, project.read_artifact/delete_artifact, script.subscribe/unsubscribe + script.event, project_id on ScriptEvent variants, gateway forwarding + script-source restart. Frontend: ScriptList/ScriptEditor/ScriptErrorPane Monaco modules with system.* completion stubs. Tests added across all layers; designer JS bundle 457.75 kB. Verified cargo workspace tests, clippy --all-features -D warnings, fmt, pnpm typecheck/test, designer build:vite.
2026-04-29 09:55  claude  CODEX-U   Reviewed. Status -> merged. Spec-compliant on the brief amendments (ScriptSource raw bytes, project_id propagation, script.subscribe/event mirror of alarm pattern). All four backend pieces and three frontend pieces present and tested. **Codex's pnpm -r test verification didn't hold on Node v25.9.0** — `vite-plugin-monaco-editor@1.1.0` calls fs.rmdirSync({recursive:true}) which Node 22+ removed. **Fix applied during review** (small, local): vite.config.ts gates the plugin to `command === 'build'` (its only job is copying Monaco workers into dist/ for production); added engines: node>=20 to designer package.json. After fix all 13 designer + 35 component-library + 13 runtime-web + 20 protocol-ts tests green; designer build still 457.75 kB. v1.1 items added: unmaintained vite-plugin-monaco-editor (replace), CDN-loaded Monaco at runtime (bundle for air-gapped), simultaneous-edit conflict on scripts, .nvmrc / pinned Node in CI to prevent environment drift in submissions. **PHASE 3 CODE-COMPLETE.**
2026-04-30 11:00  claude  --        Phase 4 driver scope expansion per maintainer direction. v1 second-driver scope grows from OPC UA-only to {OPC UA, Modbus TCP/RTU, MQTT (incl. Sparkplug B), Beckhoff ADS}. Driver trait audit confirmed no extension needed (Capabilities flags + opaque TagAddress + optional TagNode browse already cover all five protocols). docs/feature-matrix.md and docs/roadmap.md updated to bump the four protocols to v1; website index.astro updated with the broader driver list and Phase 4 in-progress badge.
2026-04-30 11:00  claude  CODEX-W   Opened. Brief: crates/driver-opcua — OPC UA client driver via `opcua` crate. Address shape `ns=N;s=...`, MonitoredItem-based subscriptions, anonymous + username/password auth, browse via OPC UA Browse service. Sim harness, wiki entry, manual smoke. Cert auth out of scope for v1.
2026-04-30 11:00  claude  CODEX-X   Opened. Brief: crates/driver-modbus — Modbus TCP + RTU in one crate, two transports. Address shape `<unit>/<area>/<addr>:<count>:<type>` with explicit endianness suffixes. Master-only (slave mode post-1.0). Sim-modbus harness in TCP only (RTU coverage is unit-test).
2026-04-30 11:00  claude  CODEX-Y   Opened. Brief: crates/driver-mqtt — generic mode (raw topics with payload decoders) + Sparkplug B mode (NBIRTH alias-map + DDATA flow). rumqttc + prost. Vendored Sparkplug B protobuf. Sim-mqtt harness via rumqttd or docker mosquitto. Client cert auth + Sparkplug primary-host coordination post-1.0.
2026-04-30 11:00  claude  CODEX-Z   Opened. Brief: crates/driver-ads — Beckhoff TwinCAT ADS client. Address shape `<port>:<symbol>` (851 PLC, 350 NC, etc.). ADS device notifications, symbol upload, AMS net id resolution. ADS server mode + struct-as-single-tag post-1.0.
2026-04-28 23:55  codex   CODEX-U   Status -> in-progress. Building backend first: ScriptSource raw artifact storage, script event protocol, project_id propagation in scripting events, and gateway forwarding before Monaco UI.
2026-04-29 09:41  codex   CODEX-U   Status -> submitted. Added ScriptSource raw Python artifacts, script event streaming, gateway script-event forwarding/restart, Monaco designer editor/list/error pane, system.* completions, and tests across Rust+TS. Verified cargo workspace tests, clippy, fmt, pnpm typecheck/test, and designer Vite build (457.75 kB JS bundle).
2026-04-30 12:04  codex   CODEX-X   Status -> in-progress. Starting crates/driver-modbus plus examples/sim-modbus from the Phase 4 Modbus brief; first pass is driver-api/Rockwell reference shape, then address parsing, TCP sim harness, tests, wiki, and manual smoke docs.
2026-04-30 15:55  codex   CODEX-X   Status -> submitted. Added openwebhmi-driver-modbus, grouped polling reads, sim-modbus TCP slave, simulator-backed integration test, Modbus wiki page, feature-matrix/manual-smoke docs. Verified fmt, clippy -D warnings, package sim-tests, and full locked workspace tests.
2026-05-01 11:35  codex   CODEX-W   Status -> in-progress. Starting crates/driver-opcua plus examples/sim-opcua; first pass is resolving the opcua 0.13 client/server API, then driver, simulator, tests, wiki, and manual smoke docs.
2026-05-01 15:19  codex   CODEX-W   Status -> submitted. Added openwebhmi-driver-opcua, async-opcua-backed sim-opcua, simulator-backed read/write/browse/subscribe test, OPC UA wiki page, feature-matrix/manual-smoke docs. Verified cargo test -p openwebhmi-driver-opcua --features sim-tests.
2026-05-01 15:30  codex   CODEX-Y   Status -> in-progress. Starting crates/driver-mqtt plus examples/sim-mqtt after CODEX-W submission; first pass is generic MQTT cache/subscription flow, Sparkplug alias mapping, simulator, tests, wiki, and manual smoke docs.
2026-05-01 16:05  codex   CODEX-Y   Status -> submitted. Added openwebhmi-driver-mqtt, Sparkplug B alias-map parser, embedded rumqttd sim-mqtt, simulator-backed generic + Sparkplug integration test, MQTT wiki page, feature-matrix/manual-smoke docs.
2026-05-01 16:20  codex   CODEX-Z   Status -> in-progress. Starting crates/driver-ads after CODEX-Y submission; first pass is current ads crate API check, address/config/symbol support, simulator harness, tests, wiki, and manual smoke docs.
2026-05-01 16:40  codex   CODEX-Z   Status -> submitted. Added openwebhmi-driver-ads, sim-ads stub responder, ADS address/symbol/config support, simulator-backed read/write/browse/subscribe test, ADS wiki page, feature-matrix/manual-smoke docs. Noted real ADS wire compatibility remains pending.
2026-05-01 17:05  codex   CODEX-AA  Status -> in-progress. Starting MQTT v1 gap closure: TLS/WebSocket config, JSONPath payloads, binary big-endian decoding, broker readiness, and three-run sim-test flake gate.
2026-05-01 17:42  codex   CODEX-AA  Status -> submitted. Added rumqttc TLS/WSS/WS transport selection, JSONPath payload decoding, binary BE fixes, uncertain initial cache, active sim broker readiness, WebSocket sim coverage, docs/wiki smoke updates, and completed the 3-run MQTT sim-test gate plus workspace test/clippy.
2026-05-01 09:55  claude  CODEX-X   Reviewed. Status -> merged. First Phase 4 driver in. Spec-compliant on every callout: 11 datatypes with ABCD/CDAB endianness suffixes, FC limits enforced (125 reg / 2000 coils), exception code mapping (IllegalDataAddress→InvalidAddress; IllegalFunction/IllegalDataValue→UnsupportedType), single-vs-multiple FC selection, read-group coalescing on (unit_id, area, adjacent address), `=0.16.1` strict pin with upstream commit `b077ca3f...` recorded in wiki. ModbusClientLike trait abstraction lets unit tests run mock-driven. Sim-modbus is hand-rolled (no circular tokio-modbus-server dep) with a Notify hook for deterministic write tests. Wiki entry is the cleanest driver-wiki yet — Independent Verification Status table separating CI-validated from "pending hardware" RTU. v1.1 polish items added: no transport-level reconnect (codex flagged in wiki open questions); RTU lacks per-request timeout; gap-merge skipped (3 separate FC3 reads for addresses 100/200/300); single shared mutex serializes all I/O; no f64_le. These items share a class with the upcoming W/Y/Z reconnect concerns — likely a single bundled "driver hardening" v1.1 item. Three Phase 4 drivers remain: W (OPC UA), Y (MQTT), Z (ADS).
2026-05-01 11:35  codex   CODEX-W   Status -> in-progress. crates/driver-opcua + sim-opcua + wiki + smoke from CODEX-W brief.
2026-05-01 15:19  codex   CODEX-W   Status -> submitted. async-opcua 0.18 client + real OPC UA server sim + 4 NodeId forms + MonitoredItem subscriptions.
2026-05-01 15:55  codex   CODEX-Y   Status -> in-progress. crates/driver-mqtt with rumqttc + Sparkplug B protobuf + rumqttd in-process broker for sim.
2026-05-01 16:05  codex   CODEX-Y   Status -> submitted. Real MQTT wire protocol, real Sparkplug B protobuf decode, in-process rumqttd sim. TLS+WS deferred via UnsupportedType.
2026-05-01 16:20  codex   CODEX-Z   Status -> in-progress. crates/driver-ads on ads 0.4.4 (brief said 0.7, doesn't exist).
2026-05-01 16:40  codex   CODEX-Z   Status -> submitted. ads scaffold + JSON-line stub sim. Wiki notes it's not exercising ads crate against a real router.
2026-05-01 19:30  claude  CODEX-W   Reviewed. Status -> merged. Real wire protocol end-to-end: opcua::client::Session + MonitoredItem + Browse service; sim-opcua uses real opcua::server::ServerBuilder. async-opcua=0.18.0 strict pin; all 4 NodeId forms parsed (s/i/g/b); status code mapping (BadNodeIdUnknown→InvalidAddress); connect timeout + retry (250ms→8s). v1.1 polish: TLS verify defaults are `trust=true / verify=false` — needs a `tls_insecure` flag with explicit warn when set; NodeId Display/parse round-trip should be locked with a unit test. Three of four Phase 4 drivers in.
2026-05-01 19:45  claude  CODEX-Y   Reviewed. Status -> merged with caveats. Real rumqttc + real Sparkplug B 3.0.0 protobuf via prost + real rumqttd in-process broker. Acceptance criteria all green. **Three v1 scope gaps from the brief body didn't ship**: TLS + WebSocket transports (driver returns UnsupportedType); RawIntBe/RawFloatBe decode ASCII text not binary BE bytes (the test passes only because sim publishes "12.5" as text); json_path payload type missing entirely. Plus the integration test was flaky (failed once during review, passed retries). **Opening CODEX-AA** as focused follow-up to close the gaps before the feature-matrix can flip from "in development" to "simulator-validated". Same shape as CODEX-V was for T's brief error.
2026-05-01 20:00  claude  CODEX-Z   **Reviewed. Status -> rejected.** The submitted driver doesn't implement ADS wire protocol — opens a plain TcpStream and exchanges JSON-line frames (SimMessage/SimCommand) with the stub sim. The `ads` crate is imported only as a dead-code "version marker" function (worker.rs:252-255) so the dependency compiles. Real Beckhoff TwinCAT runtimes don't speak this dialect. **Brief error in the Claude-authored brief**: pinned `ads = "0.7"` which doesn't exist on crates.io (latest is 0.4.4, verified). Brief amended in place with corrected `=0.4.4` pin + explicit "use ads crate's wire API end-to-end" requirement. Salvageable: address parser, symbol table data structure, manual smoke step, wiki scaffold (kept for reference). Rejected files (driver.rs JSON-line impl + sim-ads JSON responder + Cargo.toml/README/feature-matrix Z entries) removed from working tree. Status returned to `open` for rework.
2026-05-01 20:00  claude  CODEX-AA  Opened. Brief: close MQTT v1 scope gaps from CODEX-Y review — TLS via rumqttc::Transport::Tls + rustls; WebSocket via Transport::Ws; json_path PayloadType variant via jsonpath-rust; binary BE payload decoders (fix the ASCII-text bug); fix the integration test flake; add Uncertain quality state for "connected but no value seen yet". Acceptance: feature-matrix MQTT entries flip from "in development" to "simulator-validated".
2026-05-01 17:05  codex   CODEX-AA  Status -> in-progress. Focused MQTT v1 gap closure after CODEX-Z rejection handoff.
2026-05-01 17:42  codex   CODEX-AA  Status -> submitted. rumqttc TCP/TLS/WS/WSS transport selection, JsonPath PayloadType with serde round-trip, RawIntBe/RawFloatBe decode 8 raw BE bytes, configured tags seeded as Quality::Uncertain before first value, sim-mqtt waits for TCP/WS listeners, integration coverage for WS + binary BE + JSONPath + Sparkplug. TLS fixture remains external/manual. 3 consecutive driver-mqtt sim-tests runs green; workspace tests + clippy --all-features -D warnings green.
2026-05-01 21:00  claude  CODEX-AA  Reviewed. Status -> merged. All five brief items delivered: TLS via Transport::tls/tls_with_default_config; WebSocket via Transport::ws/wss; JsonPath via jsonpath-rust=1.0.4 with custom serde for both wire forms (`{"json_path":"$.path"}` and `{"json_path":{"path":"..."}}`); RawIntBe/RawFloatBe decode 8 BE bytes (with new Utf8Int/Utf8Float migration variants); seed_uncertain_cache populates configured tags with Quality::Uncertain on connect. Sim-mqtt now has separate WS listener + wait_for_listener readiness. 3-run flake gate verified. Feature-matrix MQTT entries flipped to simulator-validated. v1.1 polish: tls_insecure flag is a no-op (logs warn but doesn't bypass verification — either implement or remove); default trust source not documented (rumqttc 0.25's tls_with_default_config); no CI TLS test (intentional, external Mosquitto path documented); JsonPath returns only first match; flake fix is a 100ms sleep not a real readiness poll. **Phase 4 drivers: X + W + Y all simulator-validated. Only Z (ADS) remains.**
2026-05-01 21:30  claude  CODEX-AB  Opened. Brief: 8 new components — Gauge (SVG circular gauge with warn/alarm bands), ProgressBar (horizontal/vertical fill), Slider (write-back numeric with release/live commit), Dropdown (write-back selection with options array), ToggleSwitch (write-back boolean iOS-style pill), Button (click→tag.write a configured value, optional confirm prompt), MultiState (label/color from value match table), AlarmBanner (priority-band counts subscribed via onSubscribeAlarms). Bumps the component library from 8 → 16. Independent of driver work; can run in parallel with CODEX-Z. Brief includes shared concerns (designer-mode write suppression, bad-quality visuals, propsSchema), gotchas (objectList schema type may need to be invented for Dropdown options + MultiState states; Slider commit modes; Button's bound `target` prop), and acceptance gate of <1.5 MB component-library bundle.
2026-05-01 17:05  codex   CODEX-AB  Status -> in-progress. 8 new components, objectList schema, registry+exports, tests, smoke docs.
2026-05-01 17:27  codex   CODEX-AB  Status -> submitted. All 8 components shipped; objectList schema added to designer PropertyPanel; tests at 50; bundle 67,720 bytes (well under 1.5 MB). Z explicitly held for separate real-ADS pass.
2026-05-01 22:15  claude  CODEX-AB  Reviewed. Status -> merged with Claude-applied fix. 8 components shipped + objectList schema variant + designer-mode write suppression. **Real bug caught during review**: Slider/Dropdown/ToggleSwitch hardcoded write target as literal `"value"` instead of a configurable tag path — none could write to a real PLC tag. Fix applied during merge (small, mechanical): added `tagPath?: string` prop matching NumericInput's existing pattern; default `""` falls through to `"value"` so existing tests stayed green (50/50). v1.1 polish: read/write asymmetry (binding system gives read path but write-back needs separate config) is the architectural fix; Button.writeValue typed as TagValue | TagValue[]; AlarmBanner unbounded events array; Gauge bad-quality bands stay colored; reach to 25+ components needs one more batch. Codex's discipline holding off Z for a separate real-ADS pass is the right pattern.
2026-05-01 22:30  claude  CODEX-AC  Opened. Brief: 9 components to close the v1 25+ target — Tabs (multi-page layout, keyboard nav, bindable activeTab), Modal (operator dialog with focus trap + Confirm/Cancel + Escape key + click-outside, native <dialog>), DataGrid (sortable + paginated tabular view bound to JSON-array tag, no filtering for v1), BarChart and PieChart (categorical SVG charts; PieChart supports donut mode), Card/Spinner/Divider/Stepper (smaller filler primitives). Brief flags multi-child rendering (Tabs + Modal both need RenderProps.children — verify Container's path already plumbed before committing) and DataGrid bound-data JSON-string parsing (TagValue::Array protocol type is post-1.0). Risks/gotchas section also calls out Modal focus trap via native <dialog>, BarChart negative-value baseline, PieChart all-zero edge case. Acceptance flips feature-matrix "Component library (built-in)" row to "complete (25 components)" on merge. Independent of CODEX-Z; can run in parallel.
2026-05-01 18:21  codex   CODEX-AC  Status -> in-progress. Starting component batch 3. Verified ViewRenderer already passes RenderProps.children, so Tabs/Card/Modal can reuse the existing child path.
2026-05-01 18:31  codex   CODEX-AC  Status -> submitted. Added Tabs, Modal, DataGrid, BarChart, PieChart, Card, Spinner, Divider, and Stepper; registered/exported all nine; expanded component tests to 62; updated manual smoke and feature matrix to 25 components shipped. Bundle JS total 102,400 bytes. Verified component typecheck/test/build, pnpm -r typecheck/test, and locked Rust workspace tests.
2026-05-01 17:14  codex   CODEX-AB  Status -> in-progress. Starting component-library batch 2; implementing TS/SVG components before returning to ADS because ADS needs a real wire API spike.
2026-05-01 17:27  codex   CODEX-AB  Status -> submitted. Added Gauge, ProgressBar, Slider, Dropdown, ToggleSwitch, Button, MultiState, and AlarmBanner; registered/exported all eight; added objectList schema support in the designer property panel; expanded component tests to 50 total; docs smoke steps appended. Bundle JS total 67,720 bytes. Verified component typecheck/test/build, pnpm -r typecheck/test, and locked Rust workspace tests.
2026-05-01 23:15  claude  CODEX-AC  Reviewed. Status -> merged at 6bff505.
2026-05-01 21:43  codex   CODEX-Z   Status -> in-progress. Reworking from empty tree after rejection. Implementation will use `ads` 0.4.4 client/device/symbol/notification API for production path; no JSON-line dialect.
2026-05-01 22:30  codex   CODEX-Z   Status -> submitted. Native Rust ADS client on `ads = "=0.4.4"`: <port>:<symbol> parsing, AMS NetId/source config, configurable browsed ports, symbol upload mapping, primitive encode/decode, read/write by ADS handle, ADS device notifications with handle cleanup on stream drop. Verified cargo fmt --all --check, cargo test -p openwebhmi-driver-ads (13 tests), cargo clippy -p openwebhmi-driver-ads -D warnings. Real TwinCAT 3 validation pending.
2026-05-02 00:30  claude  CODEX-Z   Reviewed pass 2. Status -> merged at f34ebd6. Real `ads` crate usage end-to-end resolved the rejection rationale (no dead-code marker; load-bearing on read/write/notification paths). Notification handle leak addressed via SubscriptionGuard::Drop. 13/13 unit tests pass; clippy clean; fmt clean (verified independently). **Implementation-merged but NOT production-validated** — no examples/sim-ads, no integration test, no real-TwinCAT validation yet. Codex's reasoning for skipping the sim is sound: a fake ADS responder would re-introduce the previous rejection's failure mode. Per maintainer direction, simulator feasibility + TwinCAT 3 smoke runbook + sumup performance risk + ads-rs upstream risk all tracked under CODEX-AD validation hardening, opened immediately. ADS counts as v1-listed but not v1-validated until CODEX-AD's TwinCAT 3 smoke passes.
2026-05-02 09:30  claude  CODEX-AF  Opened. Brief: `crates/backup` — project export to `.owhmi` archive (tar.gz with manifest.json + artifacts/ + optional historian.sqlite + alarm-journal.sqlite); import with replace or merge mode; HTTP side-channel for the actual archive transfer with one-shot 5-minute tokens; Administrator-only authorization; SQLite online-backup API for consistent snapshots during active writes; path-traversal-safe tar extraction; manifest schema_version for forward compat. Out of scope: designer UI (later task), differential backups, cloud direct-upload, encryption at rest, scheduled backups, cross-version migration. AE integration: emit audit events if AE merged when AF lands, else TODO.
2026-05-02 01:15  claude  CODEX-AE  Opened. Brief: `crates/audit-log` — SQLite-backed append-only journal for security events (auth login/logout/failure, tag writes per-user with WebSocket vs Script source attribution, project saves, user admin); wire protocol additions (audit.subscribe / audit.query mirroring alarm pattern); gateway hooks at every event source; replace existing scripting `tracing::info!` audit lines with calls into the new crate (single source of truth). Authorization: Administrator-only for subscribe/query. Retention: 90 days default, configurable. Out of scope: designer UI (CODEX-AF), external SIEM integration, tamper-evident chains.
2026-05-02 00:35  claude  CODEX-AD  Opened. Brief: ADS validation hardening as CODEX-Z closeout. Four scope items: (1) sim feasibility decision in `wiki/drivers/ads-sim-decision.md` with green/yellow/red outcomes (red = forge protocol = hard-blocked); (2) TwinCAT 3 manual-smoke runbook appended to `apps/designer/README.md` covering 9 steps from setup through reconnect/handle-leak check; (3) run the runbook against the maintainer's local TwinCAT 3 install, record results in the wiki; (4) track ADS sumup batch-read/write performance and `birkenfeld/ads-rs` upstream maintenance as v1.1 risks. No driver code changes expected unless validation surfaces a bug. All 9 components shipped, registered, exported. Bundle 102,400 bytes verified independently (under 1.5 MB cap). Tests: 62 component-library + 13 runtime-web + 13 designer + 20 protocol-ts = 108 pnpm tests green. Multi-child rendering reuses existing ViewRenderer.children path (Codex pre-flight verified). Modal uses native <dialog> for focus trap + Escape + click-outside; designer mode renders inline with badge. DataGrid parses TagValue::String JSON arrays per the post-1.0 deferral. chartPalette lifted to shared.ts addresses brief's color consistency callout. **Library now at 25 components — v1 target met.** Cargo matrix had environmental gaps (missing icons/icon.ico for Tauri designer build; Python missing for scripting integration test) but CODEX-AC ships zero Rust changes. v1.1 polish: 5 items including Designer README smoke-step splitting, Modal fallback dead-code, DataGrid sortBy effect, Stepper test RGB coupling, Tabs bad-quality warning. **Phase 4 component slice complete; only CODEX-Z (ADS rework) remains.**
2026-05-01 21:43  codex   CODEX-Z   Status -> in-progress. Reworking ADS from an empty tree after rejection; production path must use `ads` 0.4.4 client/device/symbol/notification APIs and must not revive the rejected JSON-line simulator protocol.
2026-05-01 22:30  codex   CODEX-Z   Status -> submitted. Implemented native Rust ADS client using `ads` 0.4.4 wire APIs, AMS/source config, browsed port list, symbol upload mapping, primitive read/write, ADS device notifications with handle cleanup, ADS wiki refresh, feature-matrix update, and TwinCAT 3 manual smoke steps. Verification: ADS fmt/test/clippy green; workspace clippy green excluding Tauri designer; full workspace tests blocked by existing missing Tauri icon and Python-on-PATH scripting-worker gaps.
2026-05-02 11:00  claude  CODEX-AG  Opened. Brief: workspace toolchain modernization — pin `rust-toolchain.toml` to Rust 1.95.0 (currently floats on "stable"), bump `[workspace.package]` to `edition = "2024"` and `rust-version = "1.85"`, run `cargo fix --edition --workspace --all-features`, hand-audit the diff (closure capture, never-type fallback, `gen` keyword), accept rustfmt 2024 reformatting. Out of scope: dropping `async-trait` (`Driver` is used as `Box<dyn Driver>` and native async-fn-in-trait isn't object-safe — leave TODO note for v1.1), idiom-modernization sweep, dependency bumps, clippy-rule changes. CI uses `dtolnay/rust-toolchain@stable` which honors `rust-toolchain.toml`; verify after first push or add explicit `toolchain: 1.95.0`. **Lands first, ahead of AD/AE/AF** — Codex rebases the other three onto AG's tip after merge. Risks: pinned-with-= deps (tokio-modbus, async-opcua, rumqttc/d, prost, jsonpath-rust, ads) may emit new warnings under 1.95 (scope `#[allow]` narrowly, don't unpin); Tauri macro expansion under edition 2024 sometimes lags new rustc — pause and report if it fails.
2026-05-03  codex  CODEX-AG  Submitted. Rust toolchain pinned to 1.95.0, workspace edition bumped to 2024/MSRV 1.85, edition migration and rustfmt applied, `async-trait` retained with v1.1 TODO. Validation green locally: workspace build; `cargo test --workspace --all-features --locked` three consecutive runs; clippy `-D warnings`; fmt check; `pnpm -r typecheck`; `pnpm -r test`. CI PR verification still pending.
2026-05-03 12:00  claude  CODEX-AG  Reviewed and merged. Workspace pinned to Rust 1.95.0 via `rust-toolchain.toml`; `[workspace.package]` now `edition = "2024"` + `rust-version = "1.85"`; `cargo fix --edition` applied across 19 crates. All four scope guardrails held: `async-trait` retained with v1.1 TODO at `crates/driver-api/src/trait_def.rs`, no idiom sweep, no dep bumps, no clippy-rule changes. Codex's hand-audit found no `gen` identifier conflicts; `Cargo.lock` content-equivalent. Bonus: `apps/designer/src-tauri/icons/icon.ico` generated from existing PNG (closes the AC env gap), and `examples/sim-rockwell/tests/eip_client.rs` got a minimal `let_and_return` cleanup. Independent verification: cargo fmt clean; driver-ads tests 13/13 + clippy clean; workspace tests 26/27 (1 pre-existing Python-not-on-PATH failure, not a regression); workspace clippy --all-features clean. CI run on first push to main is the deferred acceptance-criterion gate. AD's submission landed on top of this so no rebase coordination needed.
2026-05-03 12:30  claude  CODEX-AD  Reviewed and merged. Codex shipped a fourth option not in the brief and it's the right one: instead of upstream-PR / fork / hand-roll for Secure ADS TLS, delegate to the locally-installed Beckhoff TwinCAT router via `TcAdsDll.dll` FFI on Windows. Architecturally correct (Beckhoff documents Secure ADS as router-to-router TLS, not app-level TLS), license-clean (`LoadLibraryW` from system paths, no DLL redistribution), platform-gated (`#[cfg(windows)] mod twincat_router;`). Hardware-validated against CX-23F092 (192.168.10.100.1.1) through a Secure ADS route: 14 symbols browsed, BOOL/INT/REAL/STRING reads, REAL write/read-back, 24 update events over 3 seconds. Both decision docs shipped (`wiki/drivers/ads-{tls,sim}-decision.md`); 9-step TwinCAT 3 runbook in `apps/designer/README.md`; `examples/twincat-smoke/README.md` with the minimal PLC source; hardware validation log added; sumup + ads-rs upstream-health Open Questions tracked. **Honest 🟠 finding**: Windows-router updates use 250ms polling instead of native notifications — Codex flagged this themselves, tracked as **CODEX-AH** with full brief on disk. v1.0-acceptable (in-family with Modbus/OPC UA defaults) but not undersold as v1.1 polish per CLAUDE.md. ADS feature-matrix row flips to '🟢 v1 (Phase 4, hardware-validated)'. Pre-1.0 24h soak gate remains separate.
2026-05-03  codex  CODEX-AH  Submitted. TcAdsDll native device notifications wired through `AdsSyncAddDeviceNotificationReqEx`/`AdsSyncDelDeviceNotificationReqEx`; polling removed from the Windows router subscription path; hUser uses a u32 registry key, trampoline catches panics, and DLL-thread dispatch only calls sync `UnboundedSender::send()`. Unit/workspace validation green on three runs, live CX notification smoke passed, and 50 connect/subscribe/drop cycles completed without handle exhaustion.
2026-05-03  codex  CODEX-AE  Submitted. Added `crates/audit-log` SQLite security journal, audit.subscribe/audit.query protocol, TS mirrors, gateway hooks for auth/tag/project/user events, script-write audit via `GatewayTagWriteSink`, and removed the old scripting tracing audit line. Focused Rust tests/clippy and protocol TS typecheck green.
2026-05-03  codex  CODEX-AF  Submitted. Added `crates/backup` `.owhmi` tar.gz export/import, schema_version 1 manifest, path traversal rejection, replace-mode orphan removal, optional historian/alarm SQLite online-backup snapshots, AE audit hooks, backup/restore protocol types, and gateway HTTP side-channel with one-shot 5-minute download tokens plus admin bearer-auth restore. Crate and gateway side-channel integration tests green.
2026-05-03 13:00  claude  CODEX-AH  Reviewed and merged. Native ADS device notifications wired through Windows `TcAdsDll.dll` FFI: `AdsSyncAddDeviceNotificationReqEx` + `AdsSyncDelDeviceNotificationReqEx` + `PAdsNotificationFuncEx` callback trampoline. Race-free dispatch via `Mutex<HashMap<u32, Arc<NotificationContext>>>` registry keyed by `hUser` (not `hNotification`) — registry insert happens before `add_notification` call, so callback can fire immediately without losing context. Trampoline panic-safe via `catch_unwind(AssertUnwindSafe(...))`. All-or-nothing rollback if any single `add_notification` fails (cleans up prior registrations). Drop-cleanup logs via `tracing::warn!` on DLL errors but doesn't panic. Polling fallback (`thread::spawn` + `Duration::from_millis`) fully removed from the Windows path. Five tests added (registry round-trip, dispatch decode, panic swallow, stale ID, ABI layout — the layout test is bonus over the brief and asserts `size_of` + `offset_of` byte-for-byte against Beckhoff's expected struct ABI). All 18 driver-ads tests pass; clippy clean. **Owned brief error**: my AH brief said `nTransMode = 3` for ServerOnChange; Beckhoff's `AdsDef.h` defines `ADSTRANS_SERVERONCHA = 4` (3 is `ADSTRANS_SERVERCYCLE`). Codex caught and corrected against primary source. Second time Codex has done better than the brief asked (first: AD's fourth-option TLS solution). **Hardware closeout gate landed**: native notifications observed in trace logs against CX-23F092; 50 connect/subscribe/drop reconnect cycles completed without notification-handle exhaustion. Wiki runbook table flips: `Update stream` → pass; `Handle leak check` → pass. Open Questions trimmed to genuine v1.1+ items only (reconnect recovery, sumup, route discovery, structured types, CI sim, ads-rs upstream). v1.0 ADS surface now complete pending only pre-1.0 24h soak gate.
2026-05-03 13:30  claude  CODEX-AE  Reviewed and merged (bundled with AF — gateway integration is shared between the two; splitting would require surgical reverts across 6 files and a non-compiling intermediate state). `crates/audit-log` is clean: SQLite append-only journal, schema_version=1 + meta table, both indexes, MAX_LIMIT=1000, broadcast-channel live subscribe, retention vacuum on open + explicit method, reuses existing TagValue + Role types per brief. **Comprehensive gateway integration**: AuthLogin (3 sites covering success + 2 failure paths), AuthLogout, TagWrite (5 sites covering WS source success/failure modes), ProjectSave, UserAdmin (create/delete/role-change), AuditSubscribe broadcast, AuditQuery — all with administrator-role check BEFORE consulting the audit store. `tracing::info!` line removed from `scripting/worker.rs`; single audit path via `GatewayTagWriteSink::with_audit_log`. CLI flags `--audit-db` + `--audit-retention-days` (default 90) wired in main. New `script_writes_append_audit_events` test verifies the cross-crate plumbing. **Two flagged misses owned honestly**: (1) `AuditEvent::SessionExpired` variant exists but is never emitted from the gateway — the auth-side rejection path needs a hook; v1.1 follow-up. (2) `wiki/protocol/audit.md` not created (Codex shipped `wiki/architecture/audit-log.md` instead); rustdoc + protocol-ts types fill the gap; v1.1 polish. **v1.1 polish list**: SQL-side filter pushdown (currently reads all rows then filters in Rust — Codex's wiki page acknowledges this), channel+writer-task append pattern for hot-path safety, explicit retention-vacuum unit test.
2026-05-03 14:00  claude  CODEX-AF  Reviewed and merged (bundled with AE). Export half is solid: SQLite online backup API for consistent snapshots, hand-rolled tar (~30 lines, no `tar` crate dep), hardened path validation (`..` / absolute / drive-letter / leading-slash all rejected, dedicated `malicious_archive_path_is_rejected` test proves `../evil` payload doesn't escape), schema_version=1 with explicit unsupported-version error. Gateway HTTP side-channel: `GET /api/projects/{id}/backup/{token}` with UUID token, single-use, 5-minute TTL, **bound to issuing session's peer IP** (token leakage risk mitigated). `POST /api/projects/{id}/restore` with administrator bearer auth at 1443. WS-level `authorize_admin` check BEFORE consulting backup module. Replace mode wipes target via `delete_project()`; AE audit hooks (`AuditEvent::ProjectExport`/`ProjectImport`) emit on success. Integration test `project_backup_exports_downloads_restores_over_http_side_channel` passes end-to-end. **Real 🟠 finding owned**: historian and alarm-journal data is EXPORTED but NOT IMPORTED — `import_project()` extracts the SQLite blobs from the tarball but never writes them to a target store; function signature doesn't even take `&HistorianStore` / `&AlarmJournal`. Operators get all project artifacts back on restore but lose every historian sample. Brief explicitly required this round-trip ("every artifact, historian row, and alarm journal entry round-trips"). **CODEX-AI opened in this commit** as a v1.0 closeout follow-up: extend RestoreOptions, add restore_from_path() to historian/alarm crates, gateway HTTP handler passes refs through. Per CLAUDE.md don't-undersell rule, this is a v1.0 blocker not v1.1 polish. **v1.1 polish callouts**: `wiki/protocol/backup.md` not created (same as AE), manifest slimmer than brief (no gateway_version/created_by/etc), module not split per brief, ustar 100-char path cap, no graceful session disconnect on replace, merge-mode referential integrity not validated, audit `mode: format!("{:?}")` couples to Rust Debug repr.
2026-05-03 14:15  claude  CODEX-AI  Opened. Brief: close CODEX-AF's v1.0 historian/alarm-journal import gap. Extend `RestoreOptions` with `historian_store: Option<HistorianStore>` + `alarm_journal: Option<AlarmJournal>`. Add `restore_from_path()` to historian/alarm-engine crates using rusqlite's online-backup API in restore direction. Gateway HTTP restore handler passes refs through to `import_project`. Replace mode wipes target historian/alarm; merge mode appends rows via SQLite ATTACH + `INSERT OR IGNORE` (recommended) or documented fallback to replace-only. Three new tests cover historian round-trip, alarm round-trip, replace-mode wipe. Out of scope: audit-log SQLite inclusion (v1.1+), differential restore, cross-version migration, selective restore. Risks: rusqlite restore API parameter ordering (don't clobber the archive), concurrent writes during restore (document pause-ingestion guidance), Windows temp-file cleanup. **v1.0 closeout blocker** — v1.0 should not tag without historian round-trip working.
2026-05-03  codex  CODEX-AI  Submitted. Wired `.owhmi` historian/alarm import into live stores via `RestoreOptions`, `HistorianStore::restore_from_path`/`merge_from_path`, `AlarmJournal::restore_from_path`/`merge_from_path`, and gateway HTTP restore pass-through. Replace mode restores full SQLite snapshots; merge mode uses ATTACH, historian tag-path remapping, and duplicate-ignore sample insert. Added store-level restore/merge tests plus backup historian round-trip, alarm round-trip, and replace-mode wipe tests.
2026-05-03 15:00  claude  CODEX-AI  Reviewed and merged. Closes AF's v1.0 historian + alarm-journal import gap. `Connection::restore(DatabaseName::Main, path, None)` for replace mode (safe documented direction); ATTACH + tag-path remap + `INSERT OR IGNORE` for historian merge — the correctness-critical detail being **join by tag_path, not tag_id** since tag_id is per-database AUTOINCREMENT. Transactions wrap both merges; DETACH always runs after; single-quote escaping on the ATTACH path; let-chains for clean conditional plumbing in `restore_sqlite_entries`. Errors during SQLite restore log via `tracing::warn!` and don't abort project artifact restore. `TEMP_COUNTER` AtomicU32 added to `temp_snapshot_path` defends against same-millisecond concurrent calls — bonus hardening beyond brief. Three new tests (historian round-trip, alarm round-trip, replace-mode wipe) plus per-crate restore_from_path/merge_from_path tests. All 8 backup tests pass; workspace clippy clean. Gateway HTTP restore handler at `server.rs:1461` passes `DEFAULT_HISTORIAN.get().cloned()` and `DEFAULT_ALARM_JOURNAL.get().cloned()` through; WS-side `ProjectImport` handler intentionally a no-op stub since AF's design routes import work through HTTP POST. Wiki updated comprehensively with new "Restore semantics" section. **One v1.1 polish callout**: alarm-journal merge appends without dedupe (no natural PK on schema); re-importing same archive twice doubles transitions. Documented honestly in wiki. **Pattern noted**: third submission this sprint where Codex executed brief cleanly + added small bonuses (TEMP_COUNTER, store-level tests in addition to integration tests). 🎉 Phase 4 ladder complete; v1.0 closeout list now: plugin SDK, performance baseline, 24h soak gate, plus polish items across AE/AF/AI verdicts.
2026-05-05 16:00  claude  CODEX-AJ  Opened. Brief: async hygiene fixes (Tier 1 from review pass 2). Three runtime-health gaps: (1) blocking rusqlite calls inside async tasks (HistorianStore::write_sample/read in recorder.rs:122 and gateway/server.rs:729; AuditLog::append/query at server.rs:1108/1319; AlarmJournal::write_transition via engine.ack; UserStore::authenticate at server.rs:373) — wrap each call site in tokio::task::spawn_blocking, audit-append uses outer tokio::spawn so the WS hot path does not queue on the blocking pool; (2) gateway has no coordinated shutdown — introduce a top-level tokio_util CancellationToken + JoinSet in main.rs, every long-lived spawn selects on cancellation, drain on Ctrl-C with SHUTDOWN_GRACE = 5s before abort_all; replace the pending::<()> hack at main.rs:217-224; (3) script supervisor recv_any (host.rs:387-406) busy-polls all broadcast receivers every 10ms and silently drops Lagged — replace with tokio_stream::StreamMap<String, BroadcastStream<TagSnapshot>>, surface lag at warn! level. Scope-locked: no JoinSet adoption inside subsystems, no #[must_use]/#[track_caller] sweep, no cancel-safety doc sweep, no reserve()/Permit, no newtypes, no thiserror 2.x bump, no actor-style store refactor — those are AK/AL/AM. Three new tests: recorder non-blocking on current_thread runtime, supervisor lag visibility via tracing capture, gateway shutdown drain via test-only token entry point. Risks: spawn_blocking pool saturation on audit-append (mitigated by outer-spawn pattern), JoinError translation must NOT use ? at WS handler (would close connection), Windows has no SIGINT (use token directly in tests).
2026-05-05  codex  CODEX-AJ  Submitted. Blocking SQLite/auth work now runs through spawn_blocking at async call sites: historian recorder writes, gateway auth/history/audit/alarm/backup paths, fire-and-forget audit appends via outer tokio::spawn, and alarm-engine journal transitions. Gateway main now owns a CancellationToken + JoinSet with 5s drain and no pending::<()> shutdown placeholders. Script host fan-in now uses StreamMap<BroadcastStream<TagSnapshot>> and logs Lagged with script_id/path/skipped. Added recorder current_thread non-blocking test, script lag log-capture test, gateway token shutdown-drain test, async runtime wiki page, and designer smoke checklist step. Validation green: fmt check, clippy -D warnings, and cargo test --workspace --all-features --locked three consecutive runs. Manual Ctrl-C/SQLite integrity smoke remains for maintainer hardware/runtime session.
2026-05-05  codex   CODEX-AJ  Submitted. Blocking SQLite/auth work now runs through spawn_blocking at async call sites: historian recorder writes, gateway auth/history/audit/alarm/backup paths, fire-and-forget audit appends via outer tokio::spawn, and alarm-engine journal transitions. Gateway main now owns a CancellationToken + JoinSet with 5s drain and no pending::<()> shutdown placeholders. Script host fan-in now uses StreamMap<BroadcastStream<TagSnapshot>> and logs Lagged with script_id/path/skipped. Added recorder current_thread non-blocking test, script lag log-capture test, gateway token shutdown-drain test, async runtime wiki page, and designer smoke checklist step. Validation green: fmt check, clippy -D warnings, and cargo test --workspace --all-features --locked three consecutive runs. Manual Ctrl-C/SQLite integrity smoke remains for maintainer hardware/runtime session.
2026-05-05 17:00  claude  CODEX-AJ  Reviewed and merged. Eight uniform spawn_blocking wraps lift every blocking SQLite/bcrypt call off the runtime worker pool: historian recorder write, gateway auth login + history read + audit query + alarm ack + project export + project import HTTP, and the alarm-engine subscription transition write (the LAST being a brief miss Claude owned — the brief only contemplated the WS-handler ack route, but the subscription task itself writes a transition every fire/clear; Codex caught it and made AlarmRuntime::evaluate_snapshot/apply/transition async to land spawn_blocking at the right call site). Audit-append uses tokio::spawn(spawn_blocking) outer-spawn so the WS hot path does not queue on the blocking pool. JoinError translated to wire-level errors via send_error, never propagated via ? (would close the WS). gateway/src/main.rs owns a single tokio_util CancellationToken + JoinSet; spawn_cancellable helper extracts the select! { _ = shutdown.cancelled() => {}, _ = work => {} } shape; drain_tasks does timeout → abort_all → final drain. pending::<()> placeholders removed. run_gateway factored out so the test drives it with future::pending::<io::Result<()>>() instead of relying on SIGINT (Windows-safe). Script host StreamMap<BroadcastStream<TagSnapshot>> with biased ordering preserved; warn_fan_in_lag helper extracted specifically for log-capture testability. Independent verification: cargo fmt clean, cargo clippy --workspace --all-targets --all-features --locked -D warnings clean, cargo test --workspace --all-features --locked green, pnpm -r typecheck + pnpm -r test green. Two yellow findings tracked as v1.1 follow-ups (also captured in new wiki/architecture/async-runtime-hygiene.md): (1) per-connection WebSocket handler tasks in run_server are NOT in the top-level JoinSet and not given a shutdown.cancelled() arm, so in-flight connections do not drain on Ctrl-C — genuine v1.1 hardening; (2) shutdown-drain test runs the mechanism with project: None / project_store: None, so the multi-task project-fixture variant remains polish. Manual Ctrl-C + PRAGMA integrity_check smoke deferred to maintainer hardware session. driver-ads SubscriptionGuard::backend gained #[allow(dead_code)] (incidental clippy fix, pre-existing dead code surfaced by --workspace -D warnings). AlarmEngineHandle::abort changed from consuming self to &mut self + handles.drain() — required by the new shutdown pattern (engine.abort() inside MutexGuard); correct API call. Phase 4 quality sweep CODEX-AK / -AL / -AM remain open per the design-system-roadmap framing.
2026-05-05 17:30  claude  CODEX-AK  Opened. Brief: Tier 2 follow-ups from review pass 2 — three items completing the cancellation discipline AJ established. (1) AbortHandle map sweep across 8 sites where JoinHandle<()> is stored solely for .abort(): AlarmEngineHandle.handles, RecorderHandle.handles, gateway/server.rs five per-connection subscription maps, driver-api supervisor.task; replace storage with HashMap<K, AbortHandle> and update spawn helpers (spawn_path, spawn_one, etc.) to return AbortHandle via JoinHandle::abort_handle(). (2) Cancel-safety doc annotations on every async fn reachable from one of the 8 tokio::select! arms in the workspace — Driver::subscribe BoxStream contract in trait_def.rs is the highest-value addition; one-line annotations elsewhere reuse tokio mpsc::Receiver::recv wording. NOT-cancel-safe functions get a // TODO(v1.1) and Codex-log report; do NOT fix inline. (3) Per-connection WebSocket handler drain (the AJ v1.1 hardening item): lift per-connection spawns from local connections JoinSet in run_server to the service-level JoinSet, pass shutdown CancellationToken into handlers, send WebSocket 1001 close frame on cancel within ~1s. Tests: extend AJ shutdown-drain with a multi-task project fixture; new per-connection drain test opens a real WS, cancels, asserts close frame within 1s. Out of scope: #[must_use] sweep (no real candidates), #[track_caller] sweep (low yield), pin! over Box::pin (audited; all 12 sites are BoxStream constructors, non-issue), newtypes/builders/non_exhaustive (CODEX-AL), thiserror 2.x / format-capture / missing-docs (CODEX-AM), structural cancel-safety fixes (separate brief). Risks: dropping JoinHandle immediately is correct but easy to miss (document the pattern at the first AbortHandle insertion site); cancel-safety annotation discipline (check the underlying primitive, do not intuit); WebSocket close-frame handshake timeout budget; run_server signature ripple from passing shutdown + tasks references.
2026-05-05  codex  CODEX-AK  Submitted. Cancel-only task storage now uses AbortHandle across alarm-engine, historian recorder, gateway WS subscription maps, audit subscriptions, and driver supervisor abort-on-drop. Per-connection WebSocket handlers now register in the service JoinSet, receive the shared shutdown token, and send close code 1001 with reason "gateway shutting down" on cancellation. Added cancel-safety docs for the select surfaces; WorkerProc::invoke_tag_change is explicitly documented as NOT cancel-safe with a v1.1 TODO. Added gateway shutdown project-fixture test and websocket close-frame drain test. Validation green: fmt/clippy three runs, workspace tests three consecutive runs, cargo doc, pnpm typecheck, pnpm test. Manual browser Ctrl-C + SQLite integrity smoke remains for maintainer runtime session.
2026-05-05  codex   CODEX-AK  Submitted. AbortHandle conversion at the 8 listed cancel-only task storage sites (alarm-engine, historian, five gateway/server.rs subscription maps, driver-api supervisor). Per-connection WebSocket shutdown drain wired through service-level JoinSet + CancellationToken: writer task selects on shutdown vs out_rx, sends WebSocket 1001 close frame with reason "gateway shutting down" inside a 1-second timeout, and the handler grants the writer a 1-second drain window before abort. Cancel-safety annotations added across the 8 select! sites, including the Driver::subscribe BoxStream trait contract; WorkerProc::invoke_tag_change documented as NOT cancel-safe with TODO(v1.1) marker rather than fixed inline. SupervisorHandle gained Drop impl plus task: Option<AbortHandle> shape so unforgotten handles abort their task. Validation green: cargo fmt --all --check x3, clippy --workspace --all-targets --all-features -D warnings x3, cargo test --workspace --all-features --locked x3, cargo doc --workspace --no-deps, pnpm -r typecheck, pnpm -r test. Two new tests: shutdown_token_drains_project_gateway_tasks (multi-task project fixture variant of the AJ shutdown drain) and shutdown_token_sends_websocket_close_frame (per-connection drain). Manual browser Ctrl-C + PRAGMA integrity_check smoke deferred to maintainer hardware/runtime session.
2026-05-05 18:00  claude  CODEX-AK  Reviewed and merged. AbortHandle conversion at all 8 sites uniform; pattern documented at first insertion site (alarm-engine/engine.rs:175 carries the "Dropping a JoinHandle does not cancel its task" comment as the brief asked). Cancel-safety annotations placed correctly across the 8 select! sites, with the highest-value addition being the Driver::subscribe BoxStream trait-level contract in driver-api/trait_def.rs:65-68. WorkerProc::invoke_tag_change correctly identified as NOT cancel-safe + TODO(v1.1) marker — no inline fix per brief discipline. Per-connection drain implementation: writer task biased-selects on shutdown vs out_rx, sends CloseFrame { Away (1001), "gateway shutting down" } inside a 1s timeout (handshake-timeout-safe); main handler loop selects on shutdown vs incoming with shutdown_requested flag for cleanup; end-of-handler grants writer a 1s drain window when shutdown_requested before abort. ServerRunConfig struct introduced to keep run_server signature manageable; back-compat preserved via serve_with_project_store_driver_handles_and_auth delegating to serve_inner_with_shutdown. TLS path also lifted to service-level JoinSet (was previously a local connections JoinSet). Codex extension beyond brief: SupervisorHandle gained Drop impl + Option<AbortHandle> shape — fixes a pre-existing leak where dropping the handle without shutdown() left the task running silently (third sprint occurrence of Codex catching an in-scope improvement the brief missed). Wiki page (wiki/architecture/async-runtime-hygiene.md) now has Current understanding entries 7 and 8 for the per-connection drain and AbortHandle pattern; the per-connection drain "Open question" from AJ is removed (resolved). Two new tests pass three consecutive runs. Independent verification: cargo fmt clean, clippy --workspace --all-targets --all-features --locked -D warnings clean, cargo test --workspace --all-features --locked green, cargo doc --workspace --no-deps green, pnpm typecheck + test green. Two yellow findings tracked as v1.1 polish: (1) SupervisorHandle::shutdown no longer awaits the task — mpsc reply provides ordering today but post-reply cleanup would be racy if added; (2) no regression test exercises the Driver::subscribe cancel-safety contract directly. Manual Ctrl-C + PRAGMA integrity_check smoke deferred to maintainer hardware session (same as AJ). Phase 4 quality sweep CODEX-AL (API polish — newtypes / builder split / non_exhaustive / cheap-clone handles) and CODEX-AM (thiserror 2.x / format-capture / missing-docs) remain queued.
2026-05-05 18:30  claude  CODEX-AL  Opened. Brief: Tier 3 SemVer-relevant API polish that should land before v1.0 tags. Three items: (1) TagPath + DriverId newtypes wrapping String, defined in crates/protocol with #[serde(transparent)] so wire format is byte-identical; replace String with the newtypes at every relevant site (protocol message variants, tag-engine, historian, alarm-engine, scripting RPC, gateway routing, audit-log payloads). TagAddress (PLC-side address) is NOT migrated — distinct concept, lives below TagPath. (2) #[non_exhaustive] sweep across every public struct/enum that may grow post-1.0: ClientMessage, ServerMessage, all 5 driver configs, AuditEvent, AuditQuery, RestoreOptions/BackupOptions, public error enums; explicitly NOT TagValue or Quality (intentionally exhaustive); every match against the message enums adds _ => warn!() arm. (3) ScriptHost cheap-clone refactor — three Arc<ScriptHost> exposures (server.rs:43/84, main.rs:238) become ScriptHost (Clone-able value type with internal Arc), matching the pattern HistorianStore/AuditLog/AlarmJournal/TagStore/ProjectStore/UserStore already follow. Out of scope: other identifier newtypes (ProjectId/ScriptId/AlarmId/SessionId/UserId — v1.1 follow-ups with same shape), driver-config Builder split (low ROI; serde-deser is dominant), thiserror = 2 / format-capture / missing-docs (CODEX-AM), store-actor refactor, newtype validation logic. Tests: wire-format round-trip for TagPath/DriverId byte-identical to current String form; #[non_exhaustive] regression check; ScriptHost::clone Arc::ptr_eq cheap-clone check; pnpm -r typecheck must stay green (TS shape unchanged). Risks: ts-rs + #[serde(transparent)] may need explicit #[ts(type = "string")] annotation; DriverHandles map keys decision; gateway path-splitting must not promote suffix to TagPath; #[non_exhaustive] is a one-way SemVer ratchet — don't annotate fixed-set state-machine enums (e.g. AlarmState); big diff (~1500-2500 lines across 15+ files) but mechanical per-site changes.
2026-05-05  codex  CODEX-AL  Submitted. Added protocol `TagPath`/`DriverId` transparent string newtypes and migrated runtime tag-path surfaces across protocol, tag-engine, historian, alarm-engine, audit-log, scripting, and gateway routing; `DriverHandles` now keys by `DriverId`, while driver-native `TagAddress` stays unchanged. Added `#[non_exhaustive]` on the named pre-v1.0 public growth surfaces and wildcard warn handling for future client messages. Refactored `ScriptHost` to a cheap-clone internal-Arc value, removing the gateway `Arc<ScriptHost>` exposures. New tests cover identifier JSON byte identity, TagPath/DriverId type separation, non-exhaustive wildcard matching, and ScriptHost clone identity. Validation green: fmt x3, clippy x3, workspace tests x3, cargo doc, pnpm typecheck, pnpm test. Manual browser/project smoke not run here; ProjectStore string identifier boundary documented as v1.1 design follow-up.
2026-05-05  codex   CODEX-AL  Submitted. TagPath + DriverId transparent string newtypes via string_newtype! macro in crates/protocol; migrated runtime tag-path/driver-id surfaces across protocol, tag-engine, historian, alarm-engine, audit-log, scripting, gateway routing. DriverHandles now keys by DriverId; PLC-side TagAddress unchanged. #[non_exhaustive] applied to ClientMessage, ServerMessage, AuditQuery, all 5 driver configs, RestoreOptions/BackupOptions, public error enums; TagValue/Quality doc-commented as intentionally exhaustive. Wildcard warn! arm for unhandled future ClientMessage variants. ScriptHost refactored into cheap-clone value type with Arc<ScriptHostInner>; gateway Arc<ScriptHost> exposures removed. New tests: identifier wire-format/type-safety + ScriptHost clone Arc::ptr_eq identity. New wiki page: api-surface-stability.md. Validation green: cargo fmt --all --check x3, clippy --workspace --all-targets --all-features -D warnings x3, cargo test --workspace --all-features --locked x3, cargo doc --workspace --no-deps, pnpm -r typecheck, pnpm -r test. Manual project-load smoke not run in this environment. Owned caveat: ProjectStore schema structs still carry tag paths/driver ids as String because openwebhmi-protocol depends on project-store for View/ArtifactKind — circular dep prevents promoting View.tag_path to TagPath; tracked as v1.1 design follow-up in the wiki page.
2026-05-05 19:00  claude  CODEX-AL  Reviewed and merged. TagPath + DriverId via string_newtype! macro; future identifier newtypes (ProjectId/ScriptId/AlarmId/SessionId/UserId v1.1 followups) drop in as one-line additions. Critical bonus beyond brief: Borrow<str> impl — without it every &str lookup against HashMap<TagPath, _> would force conversion at the call site; load-bearing for the gateway DriverHandles map keyed by DriverId. PartialOrd/Ord/From<&String>/From<&Self> are minor ergonomic adds. TagValue + Quality documented as intentionally exhaustive (per brief discipline). #[non_exhaustive] sweep covers ClientMessage, ServerMessage, AuditQuery, RestoreOptions, BackupOptions, all 5 driver configs, public error enums in auth/audit-log/driver-api; TagValue/Quality intentionally NOT annotated. Wildcard arm: _ => warn!(message = ?client_message, "unhandled client message kind") — exact pattern brief asked for. ScriptHost cheap-clone via Arc<ScriptHostInner> with task: Mutex<Option<JoinHandle>> (Mutex for interior mutability through Arc, Option for take() on shutdown — both required, both correct). Comprehensive identifier test (TypeId comparison proves TagPath != DriverId at the type level; tag_write JSON byte-identical to pre-newtype shape). ScriptHost::clone test uses Arc::ptr_eq(&host.inner, &clone.inner) — canonical cheap-clone identity check. Diff smaller than estimated (48 files, +399/-266 vs 1500-2500 brief estimate) because the macro DRYs the boilerplate and most call sites are 1-character type renames. Independent verification: cargo fmt clean, clippy --workspace --all-targets --all-features --locked -D warnings clean, cargo test --workspace --all-features --locked green, cargo doc --workspace --no-deps green, pnpm -r typecheck green (CRITICAL AL gate — protocol-ts TS shape preserved, ts-rs honored #[serde(transparent)] without manual #[ts(type = "string")]), pnpm -r test green. Codex extension beyond brief (4th occurrence this sprint after AJ alarm-engine async refactor, AK SupervisorHandle::Drop, now): honest scope-discipline on ProjectStore schema caveat — protocol re-exports project-store View/ArtifactKind, so promoting View.tag_path to TagPath would create circular crate dependency; correctly stopped at the boundary, documented 3 architectural-decision options in the wiki, tracked as v1.1 design work rather than forcing the migration through. Three v1.1 follow-ups in wiki: ProjectStore schema migration (architectural decision required first), other identifier newtypes (one-line string_newtype! additions once decided), deferred manual project-load smoke (same as AJ/AK). Phase 4 quality sweep status: AJ + AK + AL merged; CODEX-AM (thiserror = 2 + format-capture + missing-docs lint consistency) is the last queued Tier 4 task. None of AJ/AK/AL blocked v1.0; all four reviews tightly scoped, three caught in-scope brief gaps, all four merged on first review.
2026-05-05 19:30  claude  CODEX-AM  Opened. Brief: last and smallest task in the post-AG quality sweep — three narrow, mechanical, pre-v1.0 cleanups. (1) Bump thiserror = "1" to "2" at workspace Cargo.toml:42; absorb whatever build fallout appears (likely stricter #[from] semantics — minimal targeted fixes only, do NOT restructure error types). (2) Single format-capture cleanup at crates/driver-opcua/src/driver.rs:236 (the only format!("{}", x) site in the workspace; line moved from :241 after AL TagPath migration touched surrounding code) — convert to capture syntax via let-binding or to_string(). (3) Add #![deny(missing_docs)] to crates/driver-mqtt/src/lib.rs, driver-opcua/src/lib.rs, driver-ads/src/lib.rs (matching the bar driver-rockwell, driver-modbus, driver-api already meet); write doc comments for any pub items the lint exposes. Scope-locked per Codex pre-flight guidance: no broader format-capture sweep, no Arc::clone cosmetic rewrites, no reopening AL ProjectStore identifier boundary, no other dep bumps, no #![deny(missing_docs)] propagation beyond the three named crates, no clippy-rule additions. Risks: thiserror 2.x stricter #[from] type matching may fire on existing error derives (minimal fix is hand-rolled From impl, do NOT restructure enum); Cargo.lock drift bounded — only thiserror + thiserror-impl should change, anything else is a red flag; driver-opcua re-exports async-opcua types so missing-docs cascade may need ~5-10 one-liner /// Re-exported comments; driver-ads twincat_router is cfg(windows) — write the doc comments anyway. After AM merges, pivot to v1.0 closeout (plugin SDK, performance baseline, 24h hardware soak gate).
2026-05-05  codex  CODEX-AM  Submitted. Bumped workspace thiserror to 2.x with no code fallout; Cargo.lock drift is bounded to workspace crate dependency entries resolving thiserror 2.0.18. Converted the lone OPC UA format!("{}", node_id) site to capture syntax with a by-reference let binding. Added #![deny(missing_docs)] to driver-mqtt, driver-opcua, and driver-ads crate roots with one-line module docs; per-crate and workspace rustdoc are clean. Validation green: cargo build --workspace --all-features --locked, fmt x3, clippy x3, workspace tests x3, cargo doc x3, pnpm typecheck, pnpm test.
2026-05-25 16:00  claude [Opus 4.7]  CODEX-S   Backfilled stale frontmatter: status was submitted in the task file but the row was in Done at 7eff30a. Set status: merged, merge-commit: 7eff30a, last-update: 2026-04-27 20:06 claude (date from the merge commit). Drift caught by the new scripts/validate-agent-files validator on its first run.
2026-05-25 16:30  claude [Opus 4.7]  --        Agent-infrastructure sweep ported from rust-ethernet-ip v1.0.0 patterns: docs/agents/review-template.md (9-part structured contract), scripts/validate-agent-files + .githooks/pre-commit + CI gate, docs/agents/notes/ extraction (binding-write-asymmetry, mqtt-tls-in-ci, python-tag-write-routing, toolchain-drift), agent + [model] tags convention (enforced for entries dated 2026-05-17+), scripts/agent-commit specific-file-staging wrapper. Validator passes against 39 task files. Both openwebhmi-merge skills updated to reference the new template.
2026-05-25 16:45  claude [Opus 4.7]  CODEX-AN  Opened. Brief: add tamper-evident hash chain (SHA-256 prev_hash + hash columns) to crates/audit-log; ship audit-verify CLI; document which 21 CFR Part 11 clauses the current audit-log satisfies / partially satisfies / leaves to a separate brief. v1.1 brief unlocking pharma/medical-device procurement conversations. Schema migration is one-way and logs a MigrationCompleted event. Out of scope: electronic signatures (§11.50/.70/.100/.200 — separate v1.2 brief), event-class opt-in UI, hash backfilling on log rotation. Sourced from competitor feature-parity sweep — CFR21 compliance is a recurring procurement gate in regulated industries; OpenWebHMI has the infrastructure, lacks the framing + tamper evidence.
2026-05-25 16:46  claude [Opus 4.7]  CODEX-AO  Opened. Brief: Theme Editor panel in apps/designer pulling the deferred v0.2 Stretch item forward. Form-based CSS-variable editor (colors, fonts, sizing), live preview via :root variable updates (no full re-render), persists as singleton Theme artifact in project-store, runtime injects as <style> on view load. Light/dark switch with localStorage stickiness. Default theme is the source of truth in protocol-ts so designer + runtime share. Out of scope: per-view themes, theme import/export, preset libraries, custom-variable additions from UI, a11y contrast checker. Sourced from competitor feature-parity sweep — integrator-facing theme editors are a standard SCADA platform feature; OpenWebHMI components consume CSS variables but lack the editor UI.
2026-05-25 16:47  claude [Opus 4.7]  CODEX-AP  Opened. Brief: post-1.0 first-class recipes (named tag-value sets that an operator applies en bloc). New crates/recipes crate with Recipe/RecipeEntry/RecipeStore; protocol messages recipe.{list,get,save,delete,apply}; RecipeSelector runtime widget + RecipeEditor designer module; Python system.recipe.{apply,list}; audit-log RecipeSaved/Deleted/Applied events. Apply dispatches through GatewayTagWriteSink so the per-driver write-queue invariants from notes/python-tag-write-routing.md hold automatically. Apply semantics are intentionally NOT transactional (process control isn't); partial-apply outcome surface is the contract. Out of scope: S88 batch sequencing, recipe versioning, recipe import/export between projects (overlaps with AS), bulk execution, templates/inheritance. Sourced from competitor feature-parity sweep — recipes are a canonical SCADA feature for batch production and model changeover; OpenWebHMI has every supporting subsystem but no recipe concept.
2026-05-25 16:48  claude [Opus 4.7]  CODEX-AQ  Opened. Brief: docs/historian.md with capacity table (samples-per-GB at typical PLC tag widths), write-rate measurement on a real machine, SQLite 281 TB ceiling framing, retention policy story. Phase 1 audits whether retention is currently implemented (grep shows it isn't — no prune/purge/retention keywords); Phase 2 expands the brief to add a per-tag opt-in RetentionPolicy { max_age_ms, max_rows } that the recorder runs on a configurable interval. README features list gets a capacity callout. Out of scope: columnar storage, on-write downsampling, cross-historian replication, retention UI in designer. Sourced from competitor feature-parity sweep — capacity callouts are a standard SCADA marketing talking point; free marketing win for OpenWebHMI's already-SQLite historian.
2026-05-25 16:49  claude [Opus 4.7]  CODEX-AR  Opened. Brief: docs/deployment/raspberry-pi.md grounded in a real Pi 4 / Pi 5 deployment (not generic ARM advice). Covers cross-compile (preferred) + native compile paths, systemd unit sample, hardware spec (4 GB RAM recommended, SSD for production historian load, 64-bit Pi OS Bookworm minimum), networking + TLS reverse-proxy guidance, known limitations (designer is Tauri-desktop only, 32-bit Pi OS unsupported, openssl vs rustls, SD-card wear, time-sync for air-gapped deployments). Optional scripts/cross-compile-pi.sh wrapper. Out of scope: .deb package / apt repo / Pi OS image, Pi-on-CI testing, Yocto/Buildroot, GPIO bindings, kiosk-mode browser. Sourced from competitor feature-parity sweep — Raspberry Pi is a common edge-HMI runtime target across the SCADA market; OpenWebHMI's Rust gateway compiles for ARM but has no Pi-specific guidance.
2026-05-25 16:50  claude [Opus 4.7]  CODEX-AS  Opened. Brief: designer actions to export a single widget (props + bindings + styles) as a .owhmi-widget JSON file and import into another project. ExportedWidget shape with schema_version: 1 contract — v1.x exports import into v1.y where y >= x. Unknown widget type = hard error; unknown binding paths = warnings, widget still places (integrator manually rebinds). Out of scope: multi-widget/whole-view export (AF/AI backup path covers views), widget marketplaces, schema_version pre-1.0, auto-binding-rebinding, drag-from-filesystem import. Sourced from competitor feature-parity sweep — widget export/import between projects is a standard SCADA designer UX feature; OpenWebHMI's view schema round-trips as JSON already but no integrator surface for single-widget extraction.
2026-05-25 16:51  claude [Opus 4.7]  CODEX-AT  Opened. Brief: Material Design widget pack demo subset (Button, ToggleSwitch, NumericInput, Slider, Card, Dropdown, Gauge, Modal — 8 of 25 v1.0 components). Validates the parallel-pack architecture that mature SCADA platforms commonly ship (legacy + modern packs); not full 25-component parity. Default pack unchanged (no regression); MD pack opt-in via project pack: Option<String> field; pack-fallback to default with one-time per-type warning. Implementation strategy: recommended option is Tailwind/CSS-Modules per MD3 spec over wrapping @material/web (Lit interop friction with React). Depends on CODEX-AO for shared CSS-variable contract; if AT lands first, must define the shared surface. Demo example project examples/material-demo/. Out of scope: full 25-widget pack (v1.2 if demo lands cleanly), MD2/pre-MD3, custom-pack authoring guide, per-widget pack overrides, mass-migration helper. Sourced from competitor feature-parity sweep — parallel widget packs are a standard SCADA platform differentiator (a legacy pack plus a modern pack).
2026-05-25 20:35  codex [gpt-5]  CODEX-AX  Submitted. Fixed workspace package resolution for `@openwebhmi/protocol` and `@openwebhmi/component-library` by pointing package exports/types/main at source entrypoints so workspace typecheck no longer depends on stale `dist` output.
2026-05-25 20:36  codex [gpt-5]  CODEX-AW  Submitted. Stabilized gateway websocket shutdown/subscription tests by replacing sleeps and free-port probing with bound listeners and ping barriers. `cargo test -p openwebhmi-gateway --locked` passed.
2026-05-25 20:37  codex [gpt-5]  CODEX-AN  Submitted. Added audit-log hash chain, migration/backfill, verification API/CLI, wire fields, gateway conversion, tamper tests, and CFR Part 11 engineering mapping docs. `cargo test -p openwebhmi-audit-log --locked` passed.
2026-05-25 20:38  codex [gpt-5]  CODEX-AO  Submitted. Added project theme artifact support, Designer Theme module, runtime theme loading/application, docs, and tests. Build/typecheck/test matrix passed; Tauri DMG bundling required rerunning outside the sandbox and then passed.
2026-05-25 20:39  codex [gpt-5]  CODEX-AP  Left open. Did not implement recipes because current `VISION.md`/README v1.0 scope explicitly forbids MES code, including recipes, except planning notes.
2026-05-25 20:40  codex [gpt-5]  CODEX-AQ  Submitted. Added historian capacity docs plus `prune_older_than` and `prune_to_max_rows` retention primitives with tests. No local write-rate benchmark was claimed because machine-spec introspection was sandbox-blocked.
2026-05-25 20:41  codex [gpt-5]  CODEX-AR  Submitted. Added Raspberry Pi deployment guide, systemd unit, and cross-compile wrapper. Cross-compile was attempted but blocked by missing/incompatible host aarch64 C toolchain; no real Pi hardware smoke was performed, and the doc says so.
2026-05-25 20:42  codex [gpt-5]  CODEX-AS  Submitted. Added single-widget `.owhmi-widget` export/import path, schema mirrors, designer actions, validation/warnings, docs, and tests.
2026-05-25 20:43  codex [gpt-5]  CODEX-AT  Submitted. Added additive widget-pack registry, Material demo pack, theme-level pack selection, runtime binding, example project, docs, and tests. Chose hand-rolled React/CSS over `@material/web` to preserve existing prop contracts without a new Lit dependency.
2026-05-25 22:25  claude [Opus 4.7]  CODEX-AU  Opened. Brief: one-line workflow change adding apt-get install for libglib2.0-dev + libgtk-3-dev + libwebkit2gtk-4.1-dev + libsoup-3.0-dev + pkg-config to the rust CI job. Closes pre-existing failure (5+ consecutive red runs) where cargo clippy --workspace --all-targets recurses into apps/designer/src-tauri and glib-sys can't find the system library. Out of scope: caching the apt install, Tauri version bump, fixing any new clippy warnings that surface after build-script succeeds (separate follow-up). Discovered via post-push CI check on fcb6196 — my push didn't introduce the failure but exposed that the validator job runs alongside the broken jobs.
2026-05-25 22:26  claude [Opus 4.7]  CODEX-AV  Opened. Brief: one-line workflow change removing the redundant version: 9 from the pnpm/action-setup@v4 step so it reads from package.json's packageManager: pnpm@9.0.0. Closes pre-existing failure (5+ consecutive red runs) where the action exits on detecting two pnpm version specs. Out of scope: pnpm version bump, packageManager field changes, fixing any new typecheck/lint/test failures that surface after setup succeeds (separate follow-up), and the actions/checkout Node 20 deprecation warning (separate v1.1 brief when actions v5 ships). Discovered alongside CODEX-AU during post-push CI check on fcb6196.
2026-05-25 22:31  codex [gpt-5]  CODEX-AU  Status -> in-progress. Started after maintainer direction to handle AU and AV first.
2026-05-25 22:31  codex [gpt-5]  CODEX-AV  Status -> in-progress. Started after maintainer direction to handle AU and AV first.
2026-05-25 22:31  codex [gpt-5]  CODEX-AU  Status -> submitted. Added the Rust-job Tauri v2 Linux dependency install step before Rust/cache setup. Local verification: scripts/validate-agent-files passes; CI run URL and duration pending push/GitHub Actions.
2026-05-25 22:31  codex [gpt-5]  CODEX-AV  Status -> submitted. Removed the workflow pnpm version override so pnpm/action-setup@v4 reads packageManager pnpm@9.0.0. Local verification: scripts/validate-agent-files passes; CI run URL and downstream Node-step status pending push/GitHub Actions.
2026-05-25 23:07  codex [gpt-5]  CODEX-AU  CI verification on run 26423144512: Rust passed the new Tauri Linux dependency install, fmt, and clippy; failed later in cargo test on gateway integration timeout `websocket_gateway_forwards_script_events_by_project`.
2026-05-25 23:07  codex [gpt-5]  CODEX-AV  CI verification on run 26423144512: Node passed pnpm/action-setup, setup-node, and pnpm install; failed later in component-library typecheck because `@openwebhmi/protocol` did not resolve.
2026-05-25 23:25  claude [Opus 4.7]  CODEX-AU  Reviewed and merged at 4e9bc9b. Install Tauri Linux build deps step added exactly as briefed (libglib2.0-dev + libgtk-3-dev + libwebkit2gtk-4.1-dev + libsoup-3.0-dev + pkg-config), placed after checkout and before rust-cache so cache key isn't invalidated. Inline comment matches the brief's risk note about Tauri version drift. Independent CI verification on run 26423144512: Rust job install step passed in 23s, cargo fmt clean, cargo clippy --workspace --all-targets -- -D warnings PASSED (was failing before AU). Downstream cargo test --workspace --locked fails at crates/gateway/tests/integration.rs::websocket_gateway_forwards_script_events_by_project (timeout) — not an AU regression, was masked by earlier glib-sys build failure; tracked as CODEX-AW. Strong points: Codex correctly stopped at scope boundary, reported downstream failure honestly with test name + run url (Honesty rule applied); Tauri version (tauri = "2") cited specifically before picking package list. No yellow/orange/red findings.
2026-05-25 23:25  claude [Opus 4.7]  CODEX-AV  Reviewed and merged at 4e9bc9b. version: 9 block removed from pnpm/action-setup@v4; package.json packageManager: pnpm@9.0.0 unchanged. Independent CI verification on run 26423144512: Node job pnpm/action-setup PASSED (was failing before AV), actions/setup-node passed, pnpm install --frozen-lockfile passed, reached pnpm -r --if-present typecheck. Downstream typecheck fails in packages/component-library with Cannot find module '@openwebhmi/protocol' — not an AV regression, was masked by earlier pnpm-setup conflict; tracked as CODEX-AX. Codex's choice to omit the optional diagnostic pnpm --version step was reasonable and documented. Strong points: minimal surgical change, source of truth correctly left in package.json where Corepack also reads from, downstream failure flagged honestly. No yellow/orange/red findings.
2026-05-25 23:30  claude [Opus 4.7]  CODEX-AW  Opened. Brief: stabilize crates/gateway/tests/integration.rs::websocket_gateway_forwards_script_events_by_project which times out waiting for a message. Failure surfaced by CODEX-AU once glib-sys built and cargo test --workspace --locked could run. CLAUDE.md testing rules apply: no sleep/wall-clock waits; deterministic sync only (channel, oneshot, tokio::time::pause/advance, JoinHandle::await); three consecutive runs to verify; diagnose root cause before reaching for longer timeout (the flaky-test anti-pattern is explicitly prohibited). Out of scope: other crates/gateway/tests/ failures (separate follow-ups), gateway protocol rewrite, third-consecutive-run CI gate. Discovered by AU merge.
2026-05-25 23:30  claude [Opus 4.7]  CODEX-AX  Opened. Brief: fix packages/component-library typecheck which fails with Cannot find module '@openwebhmi/protocol' or its corresponding type declarations. Failure surfaced by CODEX-AV once pnpm setup unblocked. Likely root causes in order: (1) protocol-ts package.json exports/types field, (2) component-library package.json dep declaration (workspace:* vs literal version), (3) tsconfig project references missing, (4) build-order issue (protocol-ts .d.ts not generated before component-library typecheck). Diagnose first, then minimally fix. Out of scope: workspace-wide TS-references migration, package version bumps, downstream lint/test/build failures that surface once typecheck passes (separate follow-ups), Node 20 deprecation warning. Discovered by AV merge.
2026-05-25 23:55  claude [Opus 4.7]  CODEX-AP  Rejected — Claude-authored brief error. VISION.md L37 explicitly lists recipes/OEE/batch/traceability in the "won't merge" set for v1.0; implementation would violate the scope contract regardless of test status. Planning notes already exist at docs/roadmap.md L146 (Phase 5+). Codex correctly refused implementation citing VISION.md + README.md — gold-standard Honesty + Why-this-not-that discipline. Brief preserved in the task file as historical record; frontmatter status: rejected; row removed from board open table; no Done entry (no impl). Process miss owned: future Claude-authored briefs must grep VISION.md for won't-merge violations before opening (v1.1 polish for the brief-authoring conventions).
2026-05-26 03:35  claude [Opus 4.7]  CODEX-AQ  Reviewed and merged at 411f449. Independent verification: cargo test -p openwebhmi-historian --locked passed 9/9 (existing tests); cargo test -p openwebhmi-historian --test retention --locked passed 2/2 (prune_to_max_rows_keeps_newest_samples + prune_older_than_removes_samples_before_cutoff). CI run 26430352703 Rust job green for the first time in 5+ pushes; Node typecheck/lint/test green. Strong points: capacity table cites formula + planning bytes/sample; write-rate sandbox-block honestly disclosed; backup-before-prune CFR21-adjacent warning; prune_* returns usize (rows deleted) for caller verification ammo; engine-only opt-in pattern preserves no-behavior-change promise. One yellow polish: retention/backup interval should name typical default values in v1.1. Deferred: recorder auto-pruning wiring + write-rate benchmark on real hardware (both explicit out of scope per brief). No follow-ups opened.
2026-05-26 03:38  claude [Opus 4.7]  CODEX-AR  Reviewed and merged at 411f449. Independent verification: read docs/deployment/raspberry-pi.md (145 lines) + openwebhmi-gateway.service (24 lines) + scripts/cross-compile-pi.sh (14 lines); confirmed cargo build -p openwebhmi-gateway green on Linux x86 via CI run 26430352703. No Pi hardware available locally; cross-compile not re-run. Strong points: "Verification Status" section is gold-standard Honesty (Codex named exactly what was tested + the specific cross-compile failure modes); systemd unit hardening goes beyond minimum (NoNewPrivileges + ProtectSystem + ProtectHome + ReadWritePaths); Known Limits surfaces the integrator friction points upfront. Two yellow polish: systemd ExecStart flag verification would benefit from a `openwebhmi-gateway --help` CI smoke; OPENWEBHMI_PYTHON default in the unit context should be documented. Merged with explicit validation gate: real Pi 4/5 hardware smoke + cross-compile end-to-end + ExecStart flag validation all deferred to maintainer.
2026-05-26 03:40  claude [Opus 4.7]  CODEX-AY  Opened. Brief: mirror the CODEX-AU Tauri Linux deps install step into the Node job so pnpm -r --if-present build (which runs tauri build for apps/designer) finds glib-sys's system libraries. New downstream failure surfaced by CI run 26430352703 on commit 411f449 — same glib-sys / pkg-config error AU fixed for the Rust job, now in the Node job too. One-line workflow change (byte-identical apt install block to the rust job). Out of scope: caching apt install, DRY composite action (premature at 2 sites), Tauri version bump, fixing new tauri-build failures that surface after deps install, Node 20 deprecation. Discovered alongside AQ + AR merge.
2026-05-26  codex [gpt-5]  CODEX-AY  Submitted at 7505a62. Mirrored the Tauri Linux build dependency install step into the Node job before pnpm setup/install. Verification: `git diff --check` and `scripts/validate-agent-files` passed locally; CI run 26431030607 showed Node install/typecheck/lint/test passed and `pnpm -r --if-present build` reached Tauri's Linux bundling phase. The original glib-sys/pkg-config blocker is fixed. Downstream Node build failure remains at AppImage bundling: `failed to run linuxdeploy`; separate follow-up if AppImage packaging remains part of CI.
2026-05-26 19:00  claude [Opus 4.7]  CODEX-AY  Reviewed and merged at 7505a62. Independent verification: git show 7505a62 confirms byte-identical mirror of the AU install block in the node job; CI run 26431030607 Node job shows Install Tauri Linux build deps ✅, pnpm setup ✅, install ✅, typecheck ✅, lint ✅, test ✅, reaches pnpm -r --if-present build. The glib-sys / pkg-config blocker is gone (was the AY contract). Downstream failure at AppImage bundling (failed to run linuxdeploy) is not an AY regression — Tauri release binary + .deb + .rpm all built first; tracked as CODEX-AZ. Strong points: surgical change, scope discipline mirrors AU/AV pattern, Codex correctly resisted DRY-into-composite-action urge per brief's "wait for 3rd consumer" rule, inline comment matches AU verbatim so review trusts both jobs share the same Tauri-pin invariant note. The "fix obvious blocker → expose next layer → open narrow follow-up" pattern has now repeated 3 times (AU→AW, AV→AX, AY→AZ) — merge skill flow is working as designed.
2026-05-26 19:05  claude [Opus 4.7]  CODEX-AZ  Opened. Brief: apps/designer tauri build now fails at AppImage bundling with 'failed to run linuxdeploy' after AY unblocked the glib-sys gate. Tauri release binary + .deb + .rpm all build successfully — only .AppImage fails. Two scope-bounded fix paths: Path A install linuxdeploy + libfuse2 + --appimage-extract-and-run on the runner (keeps AppImage artifact at CI complexity cost), Path B drop AppImage from CI bundle targets via tauri build --bundles deb,rpm (recommended for CI hygiene; AppImage stays available for release-only / local builds). Out of scope: separate release workflow producing AppImage (defer until customer asks), Tauri version bump, Flatpak/Snap migration, downstream failures that surface after build exits 0, DRY of install steps across jobs (still 2 sites; wait for 3rd consumer). Discovered by AY merge CI run 26431030607.
2026-05-27  codex [gpt-5]  CODEX-AZ  Submitted. Chose Path B: CI skips AppImage by building non-designer packages normally, then running `build:vite` and `tauri build --bundles deb,rpm` for `@openwebhmi/designer`. Local/release `pnpm --filter @openwebhmi/designer build` remains unchanged, so AppImage stays available outside CI. Local verification: Tauri CLI help confirms `--bundles`; `git diff --check` and `scripts/validate-agent-files` passed. Push CI verification pending.
2026-05-27  codex [gpt-5]  CODEX-AZ  Push CI verification passed on run 26538520080 for commit bd3d006. Node job passed install/typecheck/lint/test and the new `Build packages` step; Rust and validate-agent-files also passed. The AppImage/linuxdeploy failure is removed from CI.
2026-05-26 19:25  claude [Opus 4.7]  CODEX-AW  Reviewed and merged at cc2e1f7. Independent verification: cargo test -p openwebhmi-gateway --locked green across all binaries; cargo test --test integration websocket_gateway_forwards_script_events_by_project green 3 consecutive runs (0.05s/0.05s/0.04s, no variance) per CLAUDE.md three-runs discipline for known-flaky tests; grep -rn "tokio::time::sleep|thread::sleep" in touched files returns zero matches (no-flaky-tests rule fully satisfied). Three timing patterns replaced: free-port race in unused_loopback_addr() helper, connect retry loop with sleep, sleep-then-act in shutdown + integration tests. New pattern: bind listener up front + ping-barrier (send Ping, await Pong) before publishing trigger events. Strong points: zero sleep calls survived, ping-barrier tests the real subscription handler path not an artificial wait, listener-bound-up-front eliminates the free-port race architecturally, dead helpers removed cleanly. shutdown_token_sends_websocket_close_frame restructured to gateway.abort() + .expect_err() — close-frame assertion is the test contract; clean-shutdown coverage stays in sibling shutdown_token_drains_* tests (single-responsibility split, not coverage loss). One yellow polish: Duration::from_millis(100) Pong-await timeouts could benefit from an inline "upper bound, not a delay" comment.
2026-05-26 19:30  claude [Opus 4.7]  CODEX-AX  Reviewed and merged at 47da4ea. Independent verification: git show 47da4ea confirms 8 lines / 2 package.json files, minimal change; both packages declare "private": true (safe for workspace-source-export pattern); CI runs 26430352703 + 26431030607 Node typecheck green both times post-AX (was failing pre-AX). Root cause: producer package.json exports/main/types pointed at ./dist/ paths that don't exist until pnpm build runs; consumers under pnpm strict resolution couldn't find the module without a pre-built dist/. Fix: point exports/main/types at ./src/index.ts so workspace consumers resolve to TypeScript source directly. Codex picked the simplest of the 4 brief-listed candidates (#1 + #4 combined: exports change + eliminate build-order dependency). Strong points: minimal focused change, "private": true invariant honored, identical export shape across both packages, no tsconfig or build-script regressions. One yellow polish: workspace-private convention deserves a short docs/agents/notes/workspace-package-resolution.md so future contributors know the trade-off and what would change if a package goes public.
2026-05-26 19:50  claude [Opus 4.7]  CODEX-AS  Reviewed and merged at a225aa7. Independent verification: cargo test -p openwebhmi-protocol --test widget_io --locked passed 1/1 (exported_widget_round_trips_json); pnpm --filter @openwebhmi/designer test passed 10 files / 21 tests including 5 widget-io.test.ts cases (round-trip, warnings, unknown widget type, unsupported schema version, snapshot pin). Brief contract fully met: ExportedWidget defined on both Rust + TS sides (identical field shape), #[non_exhaustive] + #[serde(default)] on Rust structs per AL convention, validateKnownWidget called both at top-level and recursively (defense in depth), no partial state on import errors (validation before addChild), id regeneration prevents collisions, snapshot test pins the v1.x JSON format. Strong points: cross-language wire-shape parity, snapshot-pinned format-stability test, hard errors for unknown widget type + unsupported schema version with helpful messages naming the offender, warnings for missing tag paths still place the widget per brief. Two yellow polish: openwebhmi_version: "0.0.1" hardcoded default (should wire to workspace version constant), Rust test could add a canonical-bytes pinned test like AN audit-log discipline. One informational green: ThemeEditor JSX render block in App.tsx landed in this commit even though it's CODEX-AO scope (AO commit 428a9cf two minutes earlier added the import + saveTheme callback + onOpenTheme prop; AS added the JSX block that uses them — code compiles in commit order, not a defect, just chunking observation worth flagging in the upcoming AO review).
2026-05-26 20:30  claude [Opus 4.7]  CODEX-AO  Reviewed and merged at 428a9cf (commit bundles AO + AT). Independent verification: cargo test -p openwebhmi-project-store --locked 7+1 passed (theme_artifact_round_trips_as_project_singleton confirms singleton-replace behavior); cargo test -p openwebhmi-backup --locked 8/8 (no regression); pnpm --filter @openwebhmi/runtime-web test 3 files/16 tests incl theme-load.test.tsx; pnpm --filter @openwebhmi/component-library test 5 files/67 tests incl theme-apply.test.tsx (proves Button consumes --primary-color CSS variable). Theme type defined identically on Rust + TS sides; DEFAULT_THEME single source of truth in protocol-ts; live preview via applyTheme() :root mutations (no full re-render); localStorage stickiness for mode; Project.theme uses serde(default, skip_serializing_if = "Option::is_none") for backward compat. Strong points beyond brief: default Button.tsx updated to use var(--primary-color, #1f4e79) so theme system affects ALL widgets not just MD pack — load-bearing AO-AT integration. Yellow polish: Rust + TS default-theme drift guard test (suggest serializing default Rust Theme + assert equals TS DEFAULT_THEME JSON), SSR-incompatibility of applyTheme() document-mutation (not a v1.0 concern). No follow-ups.
2026-05-26 20:40  claude [Opus 4.7]  CODEX-AT  Reviewed and merged at 428a9cf. Independent verification: pnpm --filter @openwebhmi/component-library test 4 material-pack cases pass (8/8 widgets render with mdc-* classes, fallback emits exactly one warning per missing type via warnedFallbacks Set, prop API parity asserted via deep-equal of propsSchema/bindableProps/defaultProps); read full 383-line material/index.tsx; read tokens.css (9-line MD3 token bridge mapping --md-sys-color-* to AO's --primary-color etc); read registry.ts getComponentDefinition refactor (additive, default registry unchanged); read examples/projects/material-demo/. Implementation strategy: Codex picked Option C (hand-rolled React/CSS) not the recommended Option 2 (Tailwind/MD3-spec) — cited "preserve existing prop contracts without a new Lit dependency" in Codex log. Honest framing trade-off: pack is MD-inspired skinning, not strict MD3 compliance (no real ripple, no elevation surfaces, no motion-system curves — just mdc-* class names + token bridge + ToggleSwitch transform). Strong points: additive registry refactor (componentRegistry unchanged), prop API parity via { ...DefaultComponent, Render({...}) } inheritance (cannot accidentally drift), MD3 token bridge in tokens.css (theme changes affect both packs uniformly), 8 widgets cover representative interaction categories (primitives + inputs + surfaces + visualization). Yellow polish: docs/widget-packs.md should reframe "Material Design widget pack" → "Material-inspired demo pack" or "MD3-styled subset" to match actual depth; no CI smoke loading examples/projects/material-demo/. No follow-ups.
2026-05-26 21:00  claude [Opus 4.7]  CODEX-AN  Reviewed and merged at 66d9e76. Independent verification: cargo test -p openwebhmi-audit-log --locked green (1 lib + 5 hash_chain + 1 CLI smoke); backup + gateway suites green (no regression). All 4 attack-vector tests pass (payload UPDATE → first_bad_id at modified row; DELETE → first_bad_id at next row; reorder → first divergent id; 100-entry base case clean). Migration test seeds 50-row pre-AN schema, opens AuditLog, verifies chain established + MigrationCompleted{rows: 50} event inserted. CLI uses env!("CARGO_BIN_EXE_audit-verify"). Strong points: CFR21 mapping doc cites eCFR source URL with date stamp and marks every clause partial or out-of-scope, NONE satisfied (Honesty rule applied at procurement-binder grade — refusing to overpromise is the strong point); tamper-evidence agent note explicitly bounds verifier guarantee to "rows present relative to stored chain" and says it does NOT prove clock/auth/e-signature/whole-DB-replacement; canonical serialization via dedicated CanonicalAuditRow struct (not serde-skip annotation) prevents key-order drift class of bugs; INSERT-then-UPDATE pattern correctly includes rowid in canonical bytes; migration idempotent via meta-key flag + crash-safe (interrupted migration's zero-hash rows get re-hashed); #[non_exhaustive] on AuditEvent honored when adding MigrationCompleted variant; sha2 = "0.10" not workspace-pinned per brief instruction. Two yellow polish: no pinned-byte test of canonical serialization (brief asked for one; Codex shipped functional tests instead — tracks tampering but doesn't pin byte representation), no debug_assert!() enforcement of single-writer SQLite mutex invariant (documented in agent note but not in code). No follow-ups opened. Closing note: largest review in the AN..AT batch by file count + behavioral depth (716 insertions / 28 deletions across 13 files), landed cleanly on first review with no defects.
2026-05-26 21:30  claude [Opus 4.7]  CODEX-AM  Reviewed and merged at 823d7a1. Independent verification: cargo build --workspace --all-features --locked clean (58.57s); cargo clippy --workspace --all-targets --all-features --locked -- -D warnings clean (1m23s; zero warnings via grep verified); cargo doc --workspace --no-deps --locked clean (1m25s; zero warnings; 21 doc pages including the 3 newly-doc-covered driver crates). Surgical 9-file / +38/-17 diff exactly matches brief scope: thiserror "1" → "2" workspace bump (no error-enum restructuring needed; thiserror 2.x stricter #[from] didn't fire on existing derives), single format-capture conversion at driver-opcua/src/driver.rs:236 via by-reference let binding, #![deny(missing_docs)] + module docs added to driver-mqtt/opcua/ads (all three now match driver-rockwell/modbus/api bar; six driver crates uniform). Cargo.lock drift bounded to 5 workspace crates flipping thiserror version; both 1.0.69 + 2.0.18 coexist as external deps still pull 1.x — expected and clean. Strong points: scope-discipline (no broader format-capture sweep, no Arc::clone rewrites, no AL ProjectStore reopening, no other dep bumps), minimum-acceptable module-doc verbosity (not overengineered), by-reference let binding for format-capture (efficient, idiomatic). One yellow polish: commit message "update of files - sergio - crates/driver- modify" is uninformative — future polish-class commits should use chore(deps): bump thiserror to 2.x shape; documented as v1.1 polish not blocking. AM closes the post-AG quality sweep (AJ + AK + AL + AM all now merged). Sitting since 2026-05-05 — 21 days of review backlog, not a quality concern.
2026-05-27 21:50  claude [Opus 4.7]  CODEX-BA  Opened. Brief: refresh apps/website/ Astro site (openwebhmi.com) to surface the AN..AR features that landed in the v1.x feature-parity sweep — CFR21 audit-log framing (top priority for pharma/medical procurement), project themes + Material widget pack, widget export/import, historian capacity story, Raspberry Pi deployment. Three pages need edits: index.astro (new "v1.x feature highlights" section + Phase 4 bullet refresh), docs.astro (new "Features & how-tos" section with 6 DocLink entries), download.astro (AppImage clarification per CODEX-AZ Path B + Pi prerequisite mention). about.astro + 404.astro stay unchanged. Static Astro/HTML/CSS only — no new deps, no new components, no new CSS classes (reuse .scope-list / .doc-grid visual patterns). Out of scope: Astro Content Collections migration (v0.2 site roadmap), Pagefind search (v0.2), interactive demo (v0.3), versioned API reference (v1.0), README.md changes (already current). Honesty discipline emphasized in risks: CFR21 framing must mirror the doc's "engineering coverage map, not a legal certification" phrasing (no "compliant"/"certified"/"validated"); Material Design = "Material-inspired demo" not strict MD3; Raspberry Pi = "documented, hardware smoke deferred to operator". Site auto-deploys to Cloudflare Workers on push to main.
2026-05-27  codex [gpt-5]  CODEX-BA  Submitted. Refreshed only `apps/website/src/pages/index.astro`, `docs.astro`, and `download.astro`: landing page now surfaces five v1.x feature cards with CFR21 audit-log framing first and updates the Phase 4 status line; docs page adds the six-link "Features & how-tos" section; download page clarifies Linux `.deb` + `.rpm` from CI and AppImage from release builds, plus a Pi 4/5 prerequisite link. Verification: `pnpm --filter @openwebhmi/website build`, `git diff --check`, `scripts/validate-agent-files`, and local HTTP content checks passed. Browser-plugin visual verification was attempted but unavailable because the in-app browser backend reported `Browser is not available: iab`.
2026-05-27 22:00  claude [Opus 4.7]  CODEX-AZ  Reviewed and merged at bd3d006. 🎉 First fully-green CI run in project history — gh run view 26538520080 confirms Node 4m2s ✅, Rust 1m49s ✅, validate-agent-files 4s ✅. Path B taken exactly as brief recommended (CI-only override via workflow file, not package.json or tauri.conf.json — local devs + release builds still produce AppImage). Three-line workflow diff: filter designer out of recursive build, then build:vite + tauri build --bundles deb,rpm for the designer. Inline comment "# CI packages deb/rpm only; AppImage remains a release/local-build target." documents the invariant. Verified apps/designer/package.json build script is unchanged; --bundles deb,rpm is Tauri v2 syntax (Codex confirmed via tauri build --help). Strong points: minimal surgical change, brief's "Don't break local developer experience" rule honored, AU→AW + AV→AX + AY→AZ "fix obvious blocker, expose next layer, open narrow follow-up" pattern closed cleanly across 6 tasks. AZ closes the CI hygiene line that had been red for 10+ consecutive prior pushes. Two yellow polish: pnpm filter syntax '!' could use an inline comment for future workflow editors (v1.2), no .AppImage from CI (v1.1+ release-only workflow brief if integrator asks). No follow-ups.
2026-05-29 18:00  claude [Opus 4.7]  CODEX-BA  Reviewed and merged at a7ba50a. Independent verification: pnpm --filter @openwebhmi/website build clean (Astro + astro check, 5 pages built in 617ms); rg -n "compliant|certified|validated for|meets 21 CFR" apps/website/src/pages returns zero matches (independently confirmed Codex's claim — no website over-claim wording); rg confirms "Material-inspired" + "hardware smoke|operator validation" phrasings honored. Brief honesty discipline applied end-to-end: CFR21 card says "engineering coverage map with every clause marked partial or out-of-scope" (matches doc's own "not a legal certification" framing); Material-inspired demo widgets (matches AT verdict reframing); Pi card says "honest hardware-smoke boundaries for operator validation". Three pages modified (index +70, docs +42, download +16); about + 404 unchanged per brief. CFR21 card leads landing-page highlights section. Strong points: minimum-diff that satisfies contract, reuses .scope-list (no new CSS), ${BLOB} template consistently used, Codex's own rg honesty self-audit documented proactively in Codex log, browser-visual honestly disclosed as "iab not available". Two yellow polish: no docs/agents/notes/website-honesty-conventions.md durable reference for future website touches, browser-visual deferred to Cloudflare preview render. BA closes Phase 4 — all 22 in-flight tasks merged (AJ/AK/AL/AM quality follow-ups + AN/AO/AQ/AR/AS/AT feature-parity + AU/AV/AW/AX/AY/AZ CI hygiene + BA website) plus AP rejected for VISION.md scope discipline. Honesty rule applied end-to-end across brief → docs → website.
2026-05-29 18:30  claude [Opus 4.7]  CODEX-BB  Opened. Brief: bump rust-toolchain.toml channel from 1.95.0 to 1.96.0, absorb whatever clippy fallout appears, convert ONE representative `match { ... => panic!() }` test site (crates/audit-log/tests/hash_chain.rs::migrates_pre_hash_schema_and_records_migration_event) to the newly-stabilized core::assert_matches::assert_matches! macro as an example pattern for future contributors. Scope-locked mirrors CODEX-AM shape: workspace Cargo.toml rust-version = "1.85" and edition = "2024" UNCHANGED (toolchain pin moves, MSRV stays); no broader test sweep; no core::range::Range adoption; no LazyCell/LazyLock From<T> ergonomic conversions; no dep bumps; no Cargo.lock churn beyond proc-macro transitives. Rust 1.96 cargo security fixes (CVE-2026-5222, CVE-2026-5223) apply automatically. New clippy lints (if any) addressed via #[expect(reason = "...")] per CLAUDE.md "Rust code quality" rule, NOT #[allow]. Honesty discipline emphasized: Codex log states exactly how many lints fired + how each was addressed; don't say "no fallout" if there was any. Out of scope: WebAssembly target change (OpenWebHMI doesn't target wasm); MSRV bump; clippy sweeps beyond new-lint addressing; restructuring the example test beyond the minimum site. Risks: CI cache invalidation on first post-bump run (Swatinem rust-cache keys on toolchain version); Cargo.lock drift bounded but not zero (proc-macro2/syn/quote may move); assert_matches! example test requires MSRV >= 1.96 to compile but acceptable since tests aren't part of published API. Estimated diff: ~5-15 lines + toolchain pin.
2026-07-12 21:24  claude [Fable 5]  --  Whole-repo review (five parallel passes: core runtime, drivers, services/security, frontend, architecture) run against main @ fa4ea58 with the full validation matrix green (cargo clippy + test --all-features, pnpm typecheck + test all pass). ~90 findings; opened 36 tasks BC..CL as the v1.0 hardening backlog (see board.md tier grouping). Headline structural finding: gateway/src/project.rs:181 calls connect_rockwell() unconditionally and never inspects driver.driver_type, so the merged Phase-4 Modbus/OPC UA/MQTT/ADS drivers are unreachable from the gateway binary and DriverSupervisor has zero consumers (CODEX-BI). Independent security convergence across passes: hardcoded fallback JWT secret (BD), ProjectStore path traversal (BC), client-steerable view ACL (BF). Docs-vs-code honesty gap: architecture.md/README/feature-matrix advertise PyO3 + system.alarm/db/http + resource sandbox + UDT/array/expression tags the code does not implement (CODEX-CI/CL).
2026-07-12 21:24  claude [Fable 5]  CODEX-BC  Opened. Tier 1 security (v1.0 blocker). Strict identifier allowlist + canonicalized-prefix check at the ProjectStore boundary for project_id + artifact ids; reject absolute/traversing script.path; validate manifest.project_id == url project_id before any backup delete/write. Closes remote path-traversal → arbitrary file R/W (store.rs:251-283, backup/lib.rs:214-224, server.rs:894-963/1676). Builds on the CODEX-AL identifier boundary. Regression tests must fail pre-fix.
2026-07-12 21:24  claude [Fable 5]  CODEX-BD  Opened. Tier 1 security (v1.0 blocker). Refuse to boot without --jwt-secret/OPENWEBHMI_JWT_SECRET unless an explicit --dev-insecure flag; kill the source-visible fallback "openwebhmi-dev-secret-rotate-immediately" (main.rs:559-562) that lets anyone forge an Administrator HS256 token. Startup-validation hard error, not a reachable panic.
2026-07-12 21:24  claude [Fable 5]  CODEX-BE  Opened. Tier 1 security (v1.0 blocker). Backup HTTP side-channel: cap request body before allocating (server.rs:1743-1756 does vec![0u8; content_length] pre-auth), verify auth from headers before reading the body (bearer check at 1632 runs after), and wrap the backup listener in the WS TlsAcceptor when TLS is configured (main.rs:138-154 plaintext leaks admin token + archive). Update the stale comment.
2026-07-12 21:24  claude [Fable 5]  CODEX-BF  Opened. Tier 1 security (HIGH). Replace the client-steerable per-connection view ACL (server.rs:1088-1124/1817-1846; None ⇒ allow, ViewClose no-op, writes unscoped to view tags) with server-derived per-tag/per-view write authorization, deny-by-default. Reconcile architecture.md:298/364/386 claims once implemented.
2026-07-12 21:24  claude [Fable 5]  CODEX-BG  Opened. Tier 1 security (HIGH). Login rate-limit + account lockout, concurrent-connection cap, max WS message size, per-connection subscription cap, unauthenticated handshake/idle timeout (server.rs:464-484 unlimited bcrypt-burning logins, 279-307 unbounded connections). Deterministic tests via tokio::time::pause; bind 127.0.0.1:0.
2026-07-12 21:24  claude [Fable 5]  CODEX-BH  Opened. Tier 1 security (HIGH; CFR21). Replace the unkeyed SHA-256 audit chain (store.rs:379-397, same-DB storage → knowledgeable tamperer recomputes and passes verify) with an HMAC keyed digest (key provisioned like BD's secret) + optional external anchor. One-way re-keying migration mirroring CODEX-AN. Keep the CFR21 coverage-map honest.
2026-07-12 21:24  claude [Fable 5]  CODEX-BI  Opened. Tier 2 drivers (HIGH, headline). Dispatch on driver.driver_type to the right Driver impl (all five implement the shared trait; connect takes serde_json::Value) + add the four missing driver crates as gateway deps + adopt DriverSupervisor backoff/panic-catch. Retires the unconditional connect_rockwell (project.rs:171-213) that makes Modbus/OPC UA/MQTT/ADS unreachable. Fail-fast on unknown type instead of the 250ms hot loop. Pairs with BJ.
2026-07-12 21:24  claude [Fable 5]  CODEX-BJ  Opened. Tier 2 drivers (HIGH, contract). Add a transport-level connection-state signal to the driver API so the gateway reconnects only on connection loss, not on any per-tag Bad (project.rs:255-258 tears down the whole driver on one bad tag; Rockwell already distinguishes PartialError vs ReadFailure at driver.rs:176-201). Land before BI wires more drivers.
2026-07-12 21:24  claude [Fable 5]  CODEX-BK  Opened. Tier 2 drivers (HIGH, hardware-gate). Bump rust-ethernet-ip 0.7 → 1.2 (maintainer-released; major jump, adapt to the new API + update wiki) and make writes target-type aware (types.rs:35-48 hardcodes Int→DINT/Real→f32; INT/SINT/LINT/LREAL writes fault on real CompactLogix), extending the existing try_from range discipline. Comment/remove the string-write workaround (driver.rs:98-100).
2026-07-12 21:24  claude [Fable 5]  CODEX-BL  Opened. Tier 2 drivers (CRITICAL). MQTT: keep polling across poll errors (driver.rs:84-87 breaks and exits, rumqttc only reconnects if you keep polling) so a broker hiccup no longer permanently freezes tags; mark cache Bad/Stale on loss (read() at 126-132 serves stale cache as Ok); stop fabricating placeholder values; log BroadcastStream Lagged.
2026-07-12 21:24  claude [Fable 5]  CODEX-BM  Opened. Tier 2 drivers (CRITICAL). ADS: replace the per-subscription recv threads racing on the cloned MPMC crossbeam notification receiver (driver.rs:121-156 → ~50% sample loss with two subscriptions / resubscribe overlap) with a single demux thread routing to a shared handle→sink registry; clean register/deregister with no overlap drop.
2026-07-12 21:24  claude [Fable 5]  CODEX-BN  Opened. Tier 2 drivers (HIGH). ADS: try_from with DriverError on out-of-range for every integer write width (symbols.rs:187-194 truncating `as` casts silently write 4464 for 70000 on INT) and make ULINT overflow consistent with Rockwell's UnsupportedType (symbols.rs:158-159 vs rockwell types.rs:18-24).
2026-07-12 21:24  claude [Fable 5]  CODEX-BO  Opened. Tier 2 drivers (HIGH). Modbus: DNS resolution via lookup_host (driver.rs:455-459 IP-literal only, docs say hostname), per-request timeout with Bad + reconnect on expiry (only connect is timed, half-open wedges read_group + the shared mutex), and bounds-checked group decode (327/337 unchecked slice panics on short device responses in the unsupervised task).
2026-07-12 21:24  claude [Fable 5]  CODEX-BP  Opened. Tier 2 drivers (HIGH). OPC UA: honor wait_for_connection() bool (driver.rs:87-89 returns Ok on dead session), delete subscriptions/monitored items on drop (178-209 leak → BadTooManySubscriptions over 24h), make SecurityPolicy/MessageSecurityMode/cert-trust configurable (61-73 hardcoded None/None), fix ns=0 + base64-padding addressing (236-237, address.rs:70-78).
2026-07-12 21:24  claude [Fable 5]  CODEX-BQ  Opened. Tier 2 drivers (MEDIUM). Sparkplug B conformance: use the datatype field to decode signed ints (sparkplug.rs:169-178 turns -1 into 4294967295, flips negative Int64 to String), handle NDEATH/DDEATH stale-marking + node-level addressing, don't discard the alias map on a valueless BIRTH metric (115-134), and issue NCMD/DCMD for writes (driver.rs:134-145 publishes to the DDATA topic string).
2026-07-12 21:24  claude [Fable 5]  CODEX-BR  Opened. Tier 3 integrity (MEDIUM). Atomic artifact writes: unique fsync'd temp + direct rename over destination (no pre-remove) + dir fsync (store.rs:503-517 removes before rename → crash loses the artifact; deterministic temp races concurrent saves). Extend the existing concurrency/stray-tmp tests in tests/store.rs.
2026-07-12 21:24  claude [Fable 5]  CODEX-BS  Opened. Tier 3 integrity (MEDIUM). Require an explicit mem/ namespace for intentional memory tags; treat other unknown-driver script writes as errors like the WS path (script_writes.rs:63-72 phantom-succeeds with Quality::Good + audit success:true vs server.rs:1387-1405). Audit enqueue vs confirmed driver result distinctly (server.rs:1407-1418). See notes/python-tag-write-routing.md.
2026-07-12 21:24  claude [Fable 5]  CODEX-BT  Opened. Tier 3 integrity (MEDIUM, SCADA safety). Drop/expire queued operator writes on disconnect instead of flushing stale setpoints to the PLC on reconnect (project.rs:90/260-278, 64-deep queue). Preserve numeric tag value type on faults (271-293 morphs numeric values to error strings; signal health via Quality).
2026-07-12 21:24  claude [Fable 5]  CODEX-BU  Opened. Tier 3 integrity (MEDIUM). Gateway lifecycle: non-fatal accept loops (server.rs:224/280, main.rs:478 kill the listener on transient errors), reliable lane for RPC replies (try_send_message:2077-2085 silently drops responses under a full queue), WS keepalive + idle + handshake timeout (329-338/409-431), and a TTL sweep for BACKUP_DOWNLOADS (47/1036-1069 leak unused export archives).
2026-07-12 21:24  claude [Fable 5]  CODEX-BV  Opened. Tier 3 integrity (LOW). Add --alarm-db/--history-db (or --data-dir) mirroring --auth-db/--audit-db; stop writing openwebhmi-alarms.sqlite/openwebhmi-history.sqlite into cwd (main.rs:327/401). Note: both are already gitignored via *.sqlite; remove the two stray untracked files from the repo root.
2026-07-12 21:24  claude [Fable 5]  CODEX-BW  Opened. Tier 4 scalability (MEDIUM). Tag-engine: shard/per-slot locking so publishes don't serialize on one global RwLock, GC never-published zero-receiver slots (subscribe:89-94 never removes slots), and one multiplexed per-connection subscription task with per-flush batching instead of task-per-(conn×tag) (server.rs:699-719/1936-1952). Add the poisoned-lock invariant comments (lib.rs:79/91/99/105). Overlaps BX in server.rs.
2026-07-12 21:24  claude [Fable 5]  CODEX-BX  Opened. Tier 4 scalability (MEDIUM). Replace the process-global OnceLock service singletons (server.rs:42-47) with an injected GatewayServices/ConnectionContext, split the ~900-line handle_connection dispatch per domain, and serialize audit appends through one bounded-mpsc appender task (append_audit:1498-1517 spawns unordered). Behavior-preserving; enables clean test injection + project reload. Overlaps BW in server.rs.
2026-07-12 21:24  claude [Fable 5]  CODEX-BY  Opened. Tier 5 frontend (HIGH). Per-node React error boundary around ViewRenderer's definition.Render (runtime-web has none; one throwing widget blanks the whole operator HMI) + a top-level boundary, with a per-widget fallback that keeps siblings live. Class-component boundary (React 18 createRoot).
2026-07-12 21:24  claude [Fable 5]  CODEX-BZ  Opened. Tier 5 frontend (HIGH). gatewayClient: tear down the old socket before reconnect (connect:112-136 leaks zombie sockets), surface dropped writes/acks (sendRaw:385-390 silently no-ops during reconnect), time out dangling requests (readHistory/readTheme:308-339 dangle forever), add request_id to ServerMessage::Error (protocol lib.rs:538-544) for correct rejection routing, use uncertain/stale not bad on reconnect, use MAX_BACKOFF_MS. Rust↔TS protocol in sync. Overlaps CA in the protocol pair.
2026-07-12 21:24  claude [Fable 5]  CODEX-CA  Opened. Tier 5 frontend (HIGH). Add ClientMessage::AlarmUnsubscribe (Rust + protocol-ts + gateway handler; client only mutates local maps at gatewayClient.ts:286-302) and stabilize the ViewRenderer context identity so AlarmTable/AlarmBanner stop re-subscribing every render (AlarmTable.tsx:85-95, AlarmBanner.tsx:44-52). Reconcile alarm state on reconnect. Overlaps BZ in the protocol pair.
2026-07-12 21:24  claude [Fable 5]  CODEX-CB  Opened. Tier 5 frontend (HIGH, refactor). Move runtime-web to per-tag subscription granularity (useSyncExternalStore keyed per path) + memoized node render + stabilized context + rAF batching, so a single tag update re-renders only its bound component(s) not the whole tree (useTagBindings.ts:31-34 + ViewRenderer.tsx:96-108). Render-count test must fail pre-refactor.
2026-07-12 21:24  claude [Fable 5]  CODEX-CC  Opened. Tier 5 frontend (HIGH). Designer integrity bundle: gate ScriptEditor autosave on the loaded id (ScriptEditor.tsx:31-78 writes script A's source into B), local-draft objectList editor (PropertyPanel.tsx:203-218 reverts invalid intermediate JSON), focus/drag guard on NumericInput/Slider (43-45/57-59) incl the material-pack clones, DesignerClient reconnect + push-after-send correlation (designerClient.ts), unique duplicate ids (viewMutations.ts:186-192).
2026-07-12 21:24  claude [Fable 5]  CODEX-CD  Opened. Tier 5 frontend (MEDIUM). Component library: consume the theme CSS variables across the default pack (only Button/material do today, so themes/dark mode don't restyle it), configurable aria-labels, role=switch + correct table roles, Slider keyboard commit on keyup/blur; apply to the material pack too. Flag the `|| "value"` write-path fallback.
2026-07-12 21:24  claude [Fable 5]  CODEX-CE  Opened. Tier 6 parity (MEDIUM). Per-alarm deadband/hysteresis + on/off delay to stop analog-alarm chattering (conditions.rs:8-22 bare comparisons, engine.rs journal+broadcast per edge). Additive definition fields (types are not #[non_exhaustive] today). Deterministic tests via tokio::time::pause. Rust↔TS in sync if wire-exposed.
2026-07-12 21:24  claude [Fable 5]  CODEX-CF  Opened. Tier 6 parity (MEDIUM). Alarm ack advances the state machine immediately using shared per-path runtime (engine.rs:70-103 only marks acked; a normalized latched require_ack alarm never clears if the tag stops updating), don't journal a transition that didn't happen, carry the real activated_at_ms (hardcoded None at :95).
2026-07-12 21:24  claude [Fable 5]  CODEX-CG  Opened. Tier 6 parity (MEDIUM). Push historian downsampling into SQL time-bucketed GROUP BY so a month of 1 Hz data doesn't pull ~2.6M rows into memory (store.rs:74-92/178-208, aggregations.rs:80-112). Values stored as JSON text → json_extract (verify JSON1). Restructure the aggregations.rs:119-120 expect. Match existing aggregation semantics on a fixture.
2026-07-12 21:24  claude [Fable 5]  CODEX-CH  Opened. Tier 6 parity (MEDIUM). Push audit from/to/user/kinds + LIMIT/OFFSET into SQL so the idx_audit_ts_user/idx_audit_kind indexes are used instead of read_all + Rust-side filter (store.rs:97-106/243-282/474-493) holding the mutex over the whole table. Add #[serde(default)] to AuditQuery.limit/offset (protocol lib.rs:145-162). Rust↔TS in sync.
2026-07-12 21:24  claude [Fable 5]  CODEX-CI  Opened. Tier 7 docs (MEDIUM, honesty; docs-only). Reconcile architecture.md/README/feature-matrix to the shipped surface: scripting is a python3 subprocess not PyO3 (no pyo3 consumer exists), only 4 RPC methods + on_tag_change, no resource sandbox; TagValue is Bool/Int/Real/String only (no UDT/array/expression tags). Fix the empty-workspace README note (21 members). Pairs with CL.
2026-07-12 21:24  claude [Fable 5]  CODEX-CJ  Opened. Tier 7 ops (MEDIUM). Add /health + /readyz + /metrics (OpenMetrics) endpoints and an opt-in JSON log layer, reusing the existing hand-rolled HTTP pattern (serve_backup_http) rather than a framework (main.rs:598-605 stderr-only, no health surface for the liveness probes architecture.md relies on).
2026-07-12 21:24  claude [Fable 5]  CODEX-CK  Opened. Tier 7 ci (MEDIUM). CI: ubuntu+macos+windows matrix, align clippy/test with CLAUDE.md's --all-features --locked (ci.yml:33-34 omits both), add a cargo-deny licenses+advisories gate (VISION.md makes MIT-compat a merge contract, currently manual), add a release workflow. Land carefully to preserve the first-ever-green CI (respect the CODEX-AZ AppImage decision).
2026-07-12 21:24  claude [Fable 5]  CODEX-CL  Opened. Tier 7 feature-gap (MEDIUM, pairs with CI). Close the documented-vs-implemented scripting gap with an explicit implement-or-defer decision per item: recommend implementing system.alarm.ack (AlarmEngine::ack exists), on_timer, system.tag.subscribe (TagStore::subscribe exists); defer system.db.query/system.http.* pending VISION.md scope check. Confirm ambiguous items in the Codex log before building.
2026-08-25 12:32  codex [gpt-5]  CODEX-BK  Started. Updating the full compatible Cargo dependency set at the maintainer's direction, moving rust-ethernet-ip to 1.2.1, adapting the Rockwell wrapper, and fixing target-type-aware writes through pre-write typed reads.
2026-08-25 12:44  codex [gpt-5]  CODEX-BK  Submitted as an uncommitted diff. rust-ethernet-ip 1.2.1, target-aware checked scalar writes, sender-context-compatible simulator, and maintainer-requested full compatible lock refresh; full Rust validation passed, hardware validation deferred.
2026-09-01  codex [gpt-5]  CODEX-CM  Opened and started. Evidence-based manufacturing capability, reference-gap, starter-demo, roadmap, and task-program documentation; reference repositories remain read-only.
2026-09-01  codex [gpt-5]  CODEX-CN  Opened. Close the v1.0 Linux/macOS/Windows Designer release-proof gap without changing the browser-only runtime scope.
2026-09-01  codex [gpt-5]  CODEX-CO  Opened, post-1.0 gated. General module contribution and module-owned SQLite storage/migration contract.
2026-09-01  codex [gpt-5]  CODEX-CP  Opened, post-1.0 gated. Versioned project templates and deterministic, explicitly simulated scenarios.
2026-09-01  codex [gpt-5]  CODEX-CQ  Opened, post-1.0 gated. Optional equipment identity plus semantic facts, projections, and quality gaps.
2026-09-01  codex [gpt-5]  CODEX-CR  Opened, post-1.0 gated. Server-authoritative action ACL, allowlist, result/actual-state, and audit command gateway.
2026-09-01  codex [gpt-5]  CODEX-CS  Opened, post-1.0 gated. Manufacturing web component pack and equipment-aware line layout.
2026-09-01  codex [gpt-5]  CODEX-CT  Opened, post-1.0 gated. Production counts/targets/model context, machine state, and quality-aware timeline.
2026-09-01  codex [gpt-5]  CODEX-CU  Opened, post-1.0 gated. Extend the existing alarm journal with mature lifecycle and equipment-aware fault analytics.
2026-09-01  codex [gpt-5]  CODEX-CV  Opened, post-1.0 gated. Reproducible report definitions, rendering, PDF/CSV, filters, and version metadata.
2026-09-01  codex [gpt-5]  CODEX-CW  Opened, post-1.0 gated. Versioned downtime policy, immutable corrections, and atomic recalculation.
2026-09-01  codex [gpt-5]  CODEX-CX  Opened, post-1.0 gated. Hierarchical quality-aware OEE after production/downtime prerequisites.
2026-09-01  codex [gpt-5]  CODEX-CY  Opened, post-1.0 gated. Incremental fictional manufacturing demo integration; initial slice does not wait for OEE.
2026-09-01  codex [gpt-5]  CODEX-CM  Submitted as an uncommitted diff. Authoritative plan, anonymized evidence/adoption matrix, demo plan, roadmap integration, Linux contract reconciliation, and CN..CY task program; agent validation, local links, privacy grep, and diff check passed.
2026-09-01  codex [gpt-5]  CODEX-CY  Maintainer clarification: a locally bundled default demo is a product-ready release gate, not an optional download; first run must offer Open Demo/Create from template offline.
2026-09-01  codex [gpt-5]  CODEX-CZ  Opened. Use deterministic scenes from the real shipped demo for reproducible website/docs screenshots, YouTube plans, release posts, social content, and presentations; no mock-only or customer-derived visuals.
2026-09-01  codex [gpt-5]  CODEX-DA  Opened. The same shipped demo supports default offline simulation and an opt-in real Rockwell PLC profile; shared logical tags/screens, explicit mode, no automatic fallback, generic controller package, and recorded hardware proof.
2026-09-01  codex [gpt-5]  CODEX-DB  Opened. First public demo video on a recorded Omarchy Linux configuration: Gateway + Designer + browser runtime, offline simulation, explicit real Rockwell profile, and honest disconnect behavior.
2026-09-01  codex [gpt-5]  CODEX-DC  Opened. Align website positioning and claims with shipped SCADA/HMI scope, explicit in-development labels, post-1.0 manufacturing direction, Linux evidence, and the planned dual-profile demo.
2026-09-01  codex [gpt-5]  CODEX-DE  Opened. Build a first-class demo hub and real-product visual story from the shipped default demo and approved reproducible captures; no mock-only features or missing-artifact calls to action.
2026-09-01  codex [gpt-5]  CODEX-DF  Opened. Add canonical/search/social/video metadata and real-product previews with claim, privacy, and no-telemetry gates.
2026-09-01  codex [gpt-5]  CODEX-DG  Opened. Specify and audit an AGPL-3.0-only product-core/MPL-2.0 protocol transition, with ownership, contribution, brand, docs/media, user-content, and plugin policy gates before any license changes.
2026-09-01  codex [gpt-5]  CODEX-DH  Opened. After DG legal/policy clearance, atomically implement AGPL core, MPL protocol exceptions, corresponding-source surfaces, artifact notices, transition cutoff, and CI enforcement; no commercial license.
2026-09-01  codex [gpt-5]  CODEX-DI  Opened. Coordinate repository documentation and website communication for the AGPL/MPL transition, including accurate commercial-use, prior-MIT, source, and no-commercial-license statements.
2026-09-01  codex [gpt-5]  CODEX-DG  Started. Launch a contributor-retains-copyright CLA with future-relicensing authority and an open-source availability promise; audit ownership/provenance and draft separate documentation, media, user-content, and trademark terms before the license transition.
2026-09-01  codex [gpt-5]  CODEX-DG  Submitted as an uncommitted diff. Ownership attestation, local/hosted contribution audit, CLA v1.0, manual PR acceptance, separate content/marks proposal, path and manifest inventory, and transition gates documented; agent validation and diff check pass. DH remains blocked on the Eclipse Tahu Sparkplug schema disposition and recorded legal reviews.
2026-09-01  codex [gpt-5]  CODEX-DJ  Opened and started. Remove the unused Eclipse Tahu-derived Sparkplug protobuf file; the MQTT crate already compiles first-party prost wire definitions and has no build consumer for the vendored schema.
2026-09-01  codex [gpt-5]  CODEX-DJ  Submitted as an uncommitted diff. Deleted the unused EPL-derived schema; current roadmap/wiki/audit now point to first-party Rust prost types. MQTT locked all-feature check and three consecutive test runs passed; formatting and agent validation passed. Initial sandboxed integration attempt could not bind a local port and was not counted.
2026-09-01  codex [gpt-5]  CODEX-DC  Started. Reconcile homepage/About/Docs/Download messaging with shipped SCADA/HMI scope, planned offline/Rockwell demo, post-1.0 manufacturing direction, and actual platform evidence; effective license claims stay MIT until DH/DI land.
2026-09-01  codex [gpt-5]  CODEX-DC  Submitted as an uncommitted diff. Homepage/About/Docs/Download now separate available, in-development, and post-1.0 direction claims; website typecheck/build and generated-route/target checks passed. Interactive visual smoke remains deferred because no in-app browser is available.
2026-09-01  codex [gpt-5]  CODEX-DG  Merged with explicit legal-review gate at 53843e5. Ownership/provenance audit, CLA, and policy specifications landed; DH remains blocked on attorney review and the transition freeze.
2026-09-01  codex [gpt-5]  CODEX-DJ  Merged at 53843e5. Removed the unused EPL-derived Sparkplug schema; full independent Rust workspace validation passed.
2026-09-01  codex [gpt-5]  CODEX-DC  Merged with explicit visual-smoke gate at 53843e5. Public website claims now separate available, in-development, and post-1.0 direction; interactive browser validation remains deferred.
2026-09-01  codex [gpt-5]  CODEX-DH  Started. Maintainer accepted proceeding without paid outside counsel; authoritative license/CLA/trademark sources are being rechecked and the decision will be recorded without implying legal review. Last MIT revision is a8cbdbc.
2026-09-01  codex [gpt-5]  CODEX-DI  Started in the coordinated transition window. Replace stale MIT claims with accurate AGPL-core/MPL-protocol, prior-revision, commercial-use, content, and unregistered-mark language.
2026-09-01 13:57  codex [gpt-5]  CODEX-DH  Merged with explicit validation gate at a8cbdbc pending transition-commit backfill. AGPL core/MPL protocols, canonical texts, offline Legal/Source surfaces, notices, transition boundaries, and CI enforcement landed. Full matrix passed; interactive browser smoke and local DMG packaging remain unproven. Maintainer accepted proceeding without outside counsel.
2026-09-01 13:57  codex [gpt-5]  CODEX-DI  Merged with explicit visual-smoke gate at a8cbdbc pending transition-commit backfill. Repository and website communication now matches the AGPL/MPL map, preserves prior MIT rights, permits commercial use under the licenses, and does not imply a commercial license or registered trademark.
