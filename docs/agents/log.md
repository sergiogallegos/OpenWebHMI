# Agent Activity Log

Append-only chronological transcript of cross-agent activity. One line per event. Newest at bottom.

Format: `YYYY-MM-DD HH:MM  <author>  <task-id-or-->  <event>`

Use `--` for task-id when the event is project-wide (protocol bootstrap, etc).

---

2026-04-26 13:00  claude  --        Cross-agent collaboration protocol bootstrapped at docs/agents/. Codex and Claude communicate via task files in tasks/, with board.md and this log as indices.
2026-04-26 13:00  claude  CODEX-A   Opened. Brief: hand-written TS counterpart of crates/protocol; vitest round-trip with literal wire-form assertions.
2026-04-26 13:00  claude  CODEX-B   Opened. Brief: Rust gateway binary, WS server + 1Hz sin-wave/counter sim provider, fans out to subscribers via TagStore.
2026-04-26 13:00  claude  CODEX-C   Opened. Brief: Vite+React+TS client, exponential-backoff reconnect, renders sin-wave + counter live. Depends on CODEX-A.
2026-04-26 13:00  claude  CODEX-D   Opened. Brief: GitHub Actions running cargo fmt/clippy/test + pnpm typecheck/lint/test on push and PR.
2026-04-26 13:40  claude  CODEX-B   Brief amended: gateway crate already listed in workspace members; CI will be red until this task lands a buildable crate. Sequencing risk noted in CODEX-D too.
2026-04-26 13:41  codex   CODEX-A   Status -> in-progress. Implementing TS protocol package from crates/protocol wire forms.
2026-04-26 13:41  codex   CODEX-B   Status -> in-progress. Implementing gateway crate, simulator provider, and WebSocket integration tests.
2026-04-26 13:41  codex   CODEX-C   Status -> in-progress. Implementing runtime client now that CODEX-A exists locally in the same workspace.
2026-04-26 13:41  codex   CODEX-D   Status -> in-progress. Implementing Phase 0 CI after A/B/C files are present.
2026-04-26 13:48  codex   CODEX-A   Status -> submitted. Added @openwebhmi/protocol package, TSDoc, type guards, and vitest wire-form tests.
2026-04-26 13:48  codex   CODEX-B   Status -> submitted. Added openwebhmi-gateway crate, sim provider, WS server, and integration test coverage.
2026-04-26 13:48  codex   CODEX-C   Status -> submitted. Added Vite React runtime and reconnecting GatewayClient tests.
2026-04-26 13:48  codex   CODEX-D   Status -> submitted. Added Phase 0 GitHub Actions workflow with locked Rust/pnpm checks.
2026-04-26 13:50  claude  --        Phase 0 bundle committed at 75ccb9c (38 files, +5439/-16). 19 cargo tests + 12 pnpm tests, all passing.
2026-04-26 13:55  claude  CODEX-A   Reviewed; spec-compliant. Status -> merged at 75ccb9c. Minor int-vs-real validation note kept on the page.
2026-04-26 13:55  claude  CODEX-B   Reviewed; spec-compliant. Status -> merged at 75ccb9c. Subscribe-time race window and forwarder panic isolation noted as Phase-1 polish.
2026-04-26 13:55  claude  CODEX-C   Reviewed; spec-compliant. Status -> merged at 75ccb9c. 3 tests cover the 6 brief'd cases (consolidation, not gap).
2026-04-26 13:55  claude  CODEX-D   Reviewed; spec-compliant. Status -> merged at 75ccb9c. CI exercised on next push.
2026-04-26 13:55  claude  CODEX-E   Opened. Brief: Driver trait, TagAddress/DriverError/DriverMetadata types, supervisor (Rust-panic-only restart), MockDriver for testing.
2026-04-26 13:55  claude  CODEX-F   Opened. Brief: driver-rockwell wraps rust-ethernet-ip v0.7.x behind Driver trait. Mocked-EipClient unit tests + simulator-gated integration tests. Blocked-by: CODEX-E.
2026-04-26 13:55  claude  CODEX-G   Opened. Brief: EtherNet/IP simulator harness; decision-then-implement (investigate rust-ethernet-ip test setup → OpenENER → custom-Rust fallback). First commit must be the wiki decision page.
2026-04-26 13:55  claude  CODEX-H   Opened. Brief: wire driver-rockwell into gateway via project-config; runtime-web renders a PLC-backed tag. Phase 1 exit-criterion demo. Blocked-by: CODEX-E, F, G.
2026-04-26 14:33  codex   CODEX-E   Status -> in-progress. Built driver-api crate, trait/types, supervisor, and mock-driver tests.
2026-04-26 14:33  codex   CODEX-G   Status -> in-progress. Investigated rust-ethernet-ip 0.7.0 simulator strategy before implementation.
2026-04-26 14:55  codex   CODEX-E   Status -> submitted. Added openwebhmi-driver-api crate with trait/types/errors/metadata/supervisor/mock and tests.
2026-04-26 14:55  codex   CODEX-G   Status -> submitted. Added simulator decision wiki page and examples/sim-rockwell harness validated by rust-ethernet-ip client test.
2026-04-26 15:01  codex   CODEX-E   Submitted amendment. Fixed default polling subscription fallback and added MockDriver coverage before review.
2026-04-26 15:17  codex   CODEX-F   Status -> in-progress. Implementing driver-rockwell against merged driver-api and sim-rockwell.
2026-04-26 16:00  claude  CODEX-E   Reviewed; spec-compliant. Status -> merged. Yellow note: supervisor recovers/reconnects on any error including caller-side InvalidAddress; gate recover() on transport-class errors before Phase 3.
2026-04-26 16:00  claude  CODEX-G   Reviewed; spec-compliant; decision-then-implement discipline exemplary. Status -> merged. Sim is bound to what rust-ethernet-ip 0.7.0 EipClient exercises; CODEX-F gaps extend the sim, not the driver workaround.
2026-04-26 16:00  claude  CODEX-F   Now unblocked (CODEX-E + CODEX-G both merged). Codex can pick up CODEX-F next.
2026-04-26 16:47  codex   CODEX-F   Status -> submitted. Added driver-rockwell wrapper, mocked EipClientLike unit tests, sim-tests integration coverage, and wiki verification updates.
2026-04-26 17:00  codex   CODEX-H   Status -> in-progress. Wiring project-file Rockwell driver subscriptions into gateway/runtime with driver status memory tags.
2026-04-26 17:08  codex   CODEX-H   Status -> submitted. Added project-file gateway driver wire-up, runtime PLC/status rows, Phase 1 demo project, and sim-tests e2e recovery coverage.
2026-04-26 20:09  codex   CODEX-I   Status -> in-progress. Building project-store crate and gateway project protocol locally.
2026-04-26 20:09  codex   CODEX-J   Status -> in-progress. Building view schema and view protocol locally because it overlaps CODEX-I protocol files.
2026-04-26 17:35  claude  CODEX-H   Reviewed; spec-compliant; Phase 1 exit criterion MET. Status -> merged. Yellow notes carried for Phase 3 polish: (1) DriverSupervisor not used (subscription-API gap); (2) single Bad update tears subscription (should distinguish per-tag PartialError from group ReadFailure); (3) fixed 250ms reconnect should inherit exponential backoff. Manual browser smoke deferred but not blocking. **PHASE 1 COMPLETE.**
2026-04-26 17:55  claude  --        v0.1.0 tagged and pushed. Annotated tag captures Phase 1 component inventory and 44-test green count.
2026-04-26 17:55  claude  CODEX-I   Opened. Brief: gateway-side project-store crate; lifts Phase 1 ad-hoc loader into its own crate; SQLite metadata + filesystem JSON; project.save_artifact / project.load / project.changed protocol additions; subscribe_changes broadcast; atomic rename for crash safety.
2026-04-26 17:55  claude  CODEX-J   Opened. Brief: View / Component / Binding schema in project-store; view.open / view.close / view.definition wire-protocol additions; TS mirror with type guards.
2026-04-26 17:55  claude  CODEX-K   Opened. Brief: 6-component library (Label, ValueDisplay, NumericInput, Indicator, Image, Container) with propsSchema-driven property panel; bad-quality visuals; canvas-compatible but no canvas behavior; vitest + RTL coverage.
2026-04-26 17:55  claude  CODEX-L   Opened. Brief: runtime-web becomes a real renderer — view loading from gateway, recursive ViewRenderer, dynamic tag binding hook, hot reload via project.changed. Blocked-by: I, J, K.
2026-04-26 17:55  claude  CODEX-M   Opened. Brief: Tauri designer shell — ConnectGateway, ProjectExplorer, TagBrowser, **form-based** ViewEditor (no canvas), PropertyPanel with debounced saves, PreviewPane via iframe to runtime. Phase 2 exit-criterion deliverable. Blocked-by: I, J, K, L.
2026-04-26 20:09  codex   CODEX-K   Status -> in-progress. Implementing packages/component-library v1 components, registry, types, workspace entry, and RTL/Vitest coverage.
2026-04-26 20:16  codex   CODEX-K   Status -> submitted. Added component-library package with 6 components, registry, dist/types, workspace entry, and passing build/typecheck/test.
2026-04-27 09:32  codex   CODEX-I   Status -> submitted. Added project-store crate, gateway project protocol, project-store on-disk wiki page, and validation/test coverage.
2026-04-27 09:32  codex   CODEX-J   Status -> submitted. Added view schema v1, Rust/TS view protocol additions, architecture docs, and round-trip/type-guard tests.
2026-04-27 10:22  codex   CODEX-L   Status -> in-progress. Replacing runtime hardcoded rows with gateway-loaded view rendering and component-library integration.
2026-04-27 10:00  claude  CODEX-I   Reviewed; spec-compliant. Status -> merged. Yellow notes: subscribe_changes ignores project_id (returns global receiver); atomic_write removes-then-renames (safer with NamedTempFile::persist); ProjectMeta save round-trips through TOML so comments/ordering are lost.
2026-04-27 10:00  claude  CODEX-J   Reviewed; spec-compliant. Status -> merged. Yellow note: protocol → project-store dep direction is non-obvious; pulls SQLite as transitive for any protocol consumer. Track for cleanup (flatten or extract a wire-types crate).
2026-04-27 10:00  claude  CODEX-K   Reviewed; spec-compliant. Status -> merged. Strong submission. Yellow notes: BindableProp uses any (cosmetic); Container bad-quality test asserts only the dashed-designer-border (functional behavior correct); committed dist/ is a one-time cleanup. CODEX-L unblocked.
2026-04-27 10:36  codex   CODEX-L   Status -> submitted. Added runtime view loading/rendering, view/project gateway client routing, dynamic tag bindings, demo home view, and verification coverage.
2026-04-27 10:43  codex   CODEX-N   Status -> in-progress. Adding `tag.write` protocol, gateway routing, and per-driver write channel before CODEX-M manual smoke.
2026-04-27 14:46  codex   CODEX-N   Status -> submitted. Added typed `tag.write`, gateway per-driver write routing, sim-backed Setpoint e2e coverage, and demo NumericInput.
2026-04-27 15:02  codex   CODEX-M   Status -> in-progress. Building required form-based designer shell; canvas/stretch work remains deferred.
2026-04-27 15:16  codex   CODEX-M   Status -> submitted. Added form-based Tauri designer, gateway save client, view mutation tests, preview iframe, and passing build/typecheck/test coverage.
2026-04-27 15:33  codex   CODEX-O   Status -> in-progress. Building historian crate, project history config, and history.read websocket API.
2026-04-27 16:24  codex   CODEX-O   Status -> submitted. Added historian crate, project history config, history.read protocol, gateway query routing, recorder filters, and aggregation tests.
2026-04-27 18:31  codex   CODEX-O   Submitted update. Wired gateway historian config reload from project tags changes and restarted recorders when existing tag rate/deadband config changes.
2026-04-27 19:27  codex   CODEX-S   Status -> in-progress. Building auth crate, JWT/bcrypt sessions, websocket auth gating, TLS flags, runtime login, and designer user-admin shell.
2026-04-27 19:52  codex   CODEX-S   Status -> submitted. Added auth crate, JWT/bcrypt users, gateway auth gating/TLS, runtime login, designer UserAdmin, protocol mirrors, and WSS smoke verification.
2026-04-27 20:07  codex   CODEX-Q   Status -> in-progress. Building alarm-engine crate, project alarm configs, alarm wire protocol, journal persistence, and gateway event routing.
2026-04-28 12:36  codex   CODEX-Q   Status -> submitted. Added alarm-engine crate, project alarm configs, alarm protocol, gateway event routing, demo pressure alarm, and verification coverage.
2026-04-28 12:38  codex   CODEX-Q   Submitted update. Same-path alarm definition hot-reload now restarts affected tag workers; added threshold-change regression coverage.
2026-04-28 14:09  codex   CODEX-R   Status -> in-progress. Building AlarmTable, runtime alarm client methods, designer alarm config module, and smoke docs.
2026-04-28 14:21  codex   CODEX-R   Status -> submitted. Added AlarmTable, runtime alarm subscribe/ack, designer alarm config authoring, ProjectExplorer Alarms entry, tests, and smoke docs.
2026-04-28 17:29  codex   CODEX-P   Status -> in-progress. Building Trend component, history.read client support, runtime history context, tests, and smoke docs.
2026-04-28 18:05  codex   CODEX-P   Status -> submitted. Added pure-SVG Trend, history.read runtime client, live append wiring, string-list tagPaths authoring, tests, and smoke docs.
2026-04-27 11:10  claude  CODEX-L   Reviewed; spec-compliant. Status -> merged. Strong submission. Yellow note: tag.write is NOT actually wired end-to-end — Phase 0 didn't ship it despite the L brief saying it had. CODEX-L correctly stayed within the brief's scope and disclosed the gap. **My brief error**, tracked as new CODEX-N.
2026-04-27 11:10  claude  CODEX-N   Opened. Brief: small focused task to add ClientMessage::TagWrite + gateway routing through a per-driver mpsc channel. Closes the brief-error from CODEX-L. Should land before CODEX-M's manual smoke so the designer's NumericInput preview is meaningful. No-blockers, runs in parallel with CODEX-M.
2026-04-27 15:00  claude  CODEX-N   Reviewed; spec-compliant; tightly scoped (no DriverSupervisor refactor — held the line per brief). Status -> merged. Per-driver bounded mpsc (capacity 64), four error codes wired (failed/unknown_driver/busy/closed-channel), Phase 1 e2e exercises full write round-trip via simulator latch. Designer NumericInput preview now meaningful. CODEX-M is the only remaining Phase 2 task.
2026-04-27 15:30  claude  CODEX-M   Reviewed; spec-compliant. Status -> merged. Required Phase 2 slice complete (form-based, no canvas). Tauri v2 .app + .dmg ship from cargo tauri build. Yellow notes: duplicateComponent withFreshIds collides on second duplicate (per-view id uniqueness violation); structuredCloneValue is JSON round-trip (fine for plain JSON, would corrupt Date/Map/Set if schema gains them); setProp shallow-merge assumes flat schema (correct today). **PHASE 2 CODE-COMPLETE.** Awaiting manual-smoke validation before v0.2.0 tag.
2026-04-27 16:00  claude  CODEX-O   Opened. Brief: crates/historian — SQLite tag time-series with (tag_id, ts) PK, history.read wire form, aggregations (raw/avg/min/max/count/sum/first/last), recorder with deadband + rate-limit, hot-reload on tags artifact change.
2026-04-27 16:00  claude  CODEX-P   Opened. Brief: Trend component (pure SVG, no chart lib), multi-pen historical fetch + live tag.update appends, dashed-segment bad quality, legend with toggle. Blocked-by: O.
2026-04-27 16:00  claude  CODEX-Q   Opened. Brief: crates/alarm-engine — AlarmDefinition with HighLimit/LowLimit/Equals/Deviation/Digital conditions, Clear→Active→Acked→Cleared state machine, SQLite journal, alarm.subscribe/event/ack wire forms.
2026-04-27 16:00  claude  CODEX-R   Opened. Brief: AlarmTable component (priority-filtered live alarm table with ack-from-UI) + designer alarm config module. Blocked-by: Q.
2026-04-27 16:00  claude  CODEX-S   Opened. Brief: crates/auth — bcrypt + jsonwebtoken, four roles (Administrator/Designer/Operator/Viewer), per-view ACLs, TLS via rustls. Login UI in runtime, UserAdmin in designer. Security-critical — no rolling our own crypto.
2026-04-27 16:00  claude  CODEX-T   Opened. Brief: crates/scripting — CPython 3.11+ via worker subprocesses (NOT in-process — subprocess isolation is the architectural commitment per architecture §4.7), JSON-RPC stdin/stdout framing, system.tag.read/write + system.util.now/log shims, on_tag_change trigger. Differentiator from Ignition's frozen Jython 2.7.
2026-04-27 16:00  claude  CODEX-U   Opened. Brief: Designer script editor — Monaco + Python syntax, system.* auto-completion stubs, debounced save, recent-errors pane. Blocked-by: T.
2026-04-27 18:31  codex   CODEX-O   Status -> submitted. Added crates/historian (SQLite store + tag dictionary interning + raw + bucketed aggregations), recorder (rate/deadband, hot reload), gateway history.read routing with ArtifactKind::Tags hot reload, Phase 1 demo rate_ms=500 on rockwell-1/Pressure.
2026-04-27 19:00  claude  CODEX-O   Reviewed; spec-compliant. Status -> merged at 9db711e (the user-requested checkpoint commit that already shipped the code). Yellow notes: Quality-via-trim_matches('"') round-trip is indirect (cosmetic; track for protocol-crate cleanup PR alongside CODEX-J's protocol→project-store dep direction note); recorder writes individual rows (fine for v1). CODEX-P unblocked.
2026-04-27 19:27  codex   CODEX-S   Status -> in-progress. Building crates/auth + gateway gating + runtime login + designer UserAdmin + rustls TLS path.
2026-04-27 19:52  codex   CODEX-S   Status -> submitted. crates/auth (bcrypt users, JWT sessions, roles, ACLs), gateway auth bootstrap + role-gated WS, runtime login + designer UserAdmin, rustls --tls-cert/--tls-key, dev-self-signed-cert.sh. WSS smoke returned HTTP/1.1 101 Switching Protocols.
2026-04-27 20:30  claude  CODEX-S   Reviewed; spec-compliant; security hygiene held throughout. Status -> merged. Yellow notes: SessionManager::new accepts secret with no minimum-length check (defense-in-depth, recommend ≥32 bytes); validation.leeway=0 stricter than default (defensible, document); User::FromStr is dead code; secret rotation deferred to v1.1 per brief. Every WS handshake now auth-aware — Phase 3 tasks Q/T build on this baseline.
2026-04-27 20:07  codex   CODEX-Q   Status -> in-progress. Building crates/alarm-engine + project-store alarm artifact + gateway forwarding/ack + Phase 1 demo pressure-high alarm.
2026-04-28 12:36  codex   CODEX-Q   Status -> submitted. Added alarm-engine (5 conditions, state machine, SQLite journal), project-store alarms artifact, gateway alarm runtime, Phase 1 demo pressure-high alarm, alarm.subscribe/event/ack wire forms.
2026-04-28 12:38  codex   CODEX-Q   Submitted update. Tightened hot-reload so same-path definition changes restart the affected tag worker; regression test for threshold lowering.
2026-04-28 13:00  claude  CODEX-Q   Reviewed; spec-compliant. Status -> merged at ff56780. Backend SCADA trio (O+Q+S) complete. Yellow notes for v1.1: no hysteresis on High/Low limits (flap risk); float Equals has IEEE-754 precision risk (use Deviation for fuzzy); CODEX-R must plumb verified session username as `who` on ack now that auth is live.
2026-04-28 14:30  claude  CODEX-R   Reviewed; spec-compliant. Status -> merged. Gateway plumbs session.username as ack `who` (server-controlled, falls back to "anonymous") — closes the CODEX-Q review note. AlarmTable: bounded buffer (default 500), priority-band + state filters, three sort keys, bad-quality `?` indicator, ack-with-note, pin-cleared. Designer AlarmConfig covers all 5 condition kinds + project.save_artifact { Alarms } with 300ms debounce. ProjectExplorer surfaces Alarms. All tests green (cargo workspace, pnpm typecheck/test). Yellow notes for v1.1: client over-fetches alarms (always sends 1..5, filters band client-side); draft↔prop race in AlarmConfig (same gotcha PropertyPanel has); no client-side id-uniqueness check on alarm definitions. Green: DMG bundling failed at create-dmg (environmental, not a regression); designer preview shows empty table (by design). Phase 3 alarm slice now end-to-end complete.
2026-04-28 18:30  claude  CODEX-P   Reviewed; spec-compliant. Status -> merged. Pure-SVG Trend, no chart library. Historical prime via history.read aggregation:"raw", live append from runtime liveValues with lastLiveTs ref dedupe. Multi-pen with 6-color rotation; bad-quality dashed via separate `<path>` elements. PropertyPanel gains stringList editor for tagPaths. ViewRenderer.collectTagPaths walks Trend.props.tagPaths so subscriptions cover them. 6 Trend tests + 1 gatewayClient + 1 ViewRenderer; all 35 component-library + 13 runtime-web pass. Yellow notes for v1.1: pathForQuality semantics drift (single-bad-sample renders as disconnected dot, not dashed segment between neighbors); paths useMemo always misses (visible filter creates fresh ref each render); live-append effect lists series in deps (render→effect cycle, ref-guarded); readHistory promises hang on disconnect (rejectPending not called from scheduleReconnect); time-axis only labels xMin/xMax (no intermediate ticks). Phase 3 trend slice complete; T (scripting host) is the only unblocked task remaining.
2026-04-28 18:22  codex   CODEX-T   Status -> in-progress. Building crates/scripting with CPython worker subprocesses, newline-delimited JSON RPC, on_tag_change dispatch, direct TagStore system.tag.read/write, and structured script-write audit logs.
2026-04-28 18:45  codex   CODEX-T   Status -> submitted. Added crates/scripting subprocess host + Python system shim, project-store ScriptConfig, script.run/result/error protocol forms, gateway script startup, and phase1-demo derived-setpoint script. Verified cargo test -p openwebhmi-scripting; cargo test --workspace --all-features --locked; cargo fmt --check; cargo clippy --workspace --all-targets --all-features -- -D warnings; pnpm -r typecheck; pnpm -r test.
2026-04-28 19:15  claude  CODEX-T   Reviewed; spec-compliant per the brief. Status -> merged at f8b74a9. Subprocess isolation (no PyO3 in gateway), \n-delimited JSON-RPC framing, 250ms→8s exponential backoff with ±25% jitter, per-handler 5s timeout, audit log INFO with script_id+path+value (closes my brief amendment). 5 host integration tests cover round-trip, crash recovery, timeout, util.log, and 4-thread concurrent RPC correlation. Phase 1 demo gains derived-setpoint.py + scripts.json. **Brief error I authored**: brief told Codex `system.tag.write` calls `TagStore::publish` directly — Codex implemented faithfully, but practical effect is scripts can only update gateway cache, not push writes to PLC. Phase 3 exit criterion isn't meaningfully met until fixed. Opening CODEX-V as a focused closeout follow-up before declaring Phase 3 done. Other yellow notes for v1.1: load-time errors don't reach ScriptEvent::Error; `recv_any` 10ms poll loop; broadcast Lagged silently dropped; HostFrame::Shutdown unused; scripts.json envelope shape.
2026-04-28 19:35  claude  CODEX-V   Opened. Brief: route `system.tag.write` through the same per-driver write mpsc that CODEX-N built for WS-side tag.write; memory-tag paths fall through to TagStore::publish. Closes the brief error from CODEX-T review. Small focused task: TagWriteSink trait + GatewayTagWriteSink impl + driver-routed test + Phase 1 manual smoke confirms Setpoint persists across poll cycles (no oscillation). Independent of CODEX-U; can run in parallel.
2026-04-28 18:50  codex   CODEX-V   Status -> in-progress. Adding TagWriteSink and gateway driver-queue routing for script-driven system.tag.write.
2026-04-28 22:55  codex   CODEX-V   Status -> submitted. Added scripting TagWriteSink + gateway GatewayTagWriteSink driver queue routing; memory tags still publish to TagStore. Added driver-sink recording and Busy-propagation tests. Verified cargo scripting/workspace tests, clippy, fmt, pnpm typecheck/test, and a live sim+gateway websocket smoke showing Setpoint persisted across 5+ poll cycles.
2026-04-28 23:15  claude  CODEX-V   Reviewed; spec-compliant. Status -> merged at 9db307e. TagWriteSink trait + TagWriteError + MemorySink (sink.rs); GatewayTagWriteSink mirrors WS-side path with split_once('/') and DriverHandles::try_write, falls through to TagStore::publish for both no-separator and unregistered-prefix paths. Audit log INFO before sink call, warn after rejection, error string propagated to Python RuntimeError (verified end-to-end in tag_write_busy_propagates_error_to_python). The new tag_write_routes_to_driver_sink test asserts both that the sink saw the write AND that store.get is None — proving the routing is real, not double-published. Existing 5 host tests preserved via MemorySink injection. 7 scripting tests + workspace tests + clippy --all-features -D warnings + pnpm green. Live sim+gateway smoke confirmed Setpoint persists across 5+ poll cycles. **Phase 3 exit criterion meaningfully met for the scripting slice.** Two small v1.1 items added: dedicated Closed-propagation test, gateway-sink driver-branch unit test. CODEX-U is the final Phase 3 task.
2026-04-28 23:45  claude  CODEX-U   Brief refreshed. Two important amendments: (1) script source is NOT saved through ArtifactKind::Script (which is the JSON config envelope) — adds a new ArtifactKind::ScriptSource { id } that resolves the path from loaded ScriptConfig and writes raw bytes; (2) the script-event subscribe protocol doesn't exist yet (CODEX-T's ScriptRun/ScriptResult/ScriptError are one-shot, request_id-correlated) — adds ClientMessage::ScriptSubscribe/ScriptUnsubscribe + ServerMessage::ScriptEvent mirroring alarm.subscribe/event. ScriptEvent enum needs project_id added to each variant for subscription filtering. Backend lands first, then frontend. Files-to-modify list refreshed; risks/gotchas updated; manual smoke tightened. Now genuinely unblocked.
2026-04-28 23:55  codex   CODEX-U   Status -> in-progress. Backend slice first: raw ScriptSource artifact, script subscribe/unsubscribe + event protocol, project_id on ScriptEvent, gateway forwarding/restart.
2026-04-29 09:41  codex   CODEX-U   Status -> submitted. Backend: ArtifactKind::ScriptSource raw-bytes storage, project.read_artifact/delete_artifact, script.subscribe/unsubscribe + script.event, project_id on ScriptEvent variants, gateway forwarding + script-source restart. Frontend: ScriptList/ScriptEditor/ScriptErrorPane Monaco modules with system.* completion stubs. Tests added across all layers; designer JS bundle 457.75 kB. Verified cargo workspace tests, clippy --all-features -D warnings, fmt, pnpm typecheck/test, designer build:vite.
2026-04-29 09:55  claude  CODEX-U   Reviewed. Status -> merged. Spec-compliant on the brief amendments (ScriptSource raw bytes, project_id propagation, script.subscribe/event mirror of alarm pattern). All four backend pieces and three frontend pieces present and tested. **Codex's pnpm -r test verification didn't hold on Node v25.9.0** — `vite-plugin-monaco-editor@1.1.0` calls fs.rmdirSync({recursive:true}) which Node 22+ removed. **Fix applied during review** (small, local): vite.config.ts gates the plugin to `command === 'build'` (its only job is copying Monaco workers into dist/ for production); added engines: node>=20 to designer package.json. After fix all 13 designer + 35 component-library + 13 runtime-web + 20 protocol-ts tests green; designer build still 457.75 kB. v1.1 items added: unmaintained vite-plugin-monaco-editor (replace), CDN-loaded Monaco at runtime (bundle for air-gapped), simultaneous-edit conflict on scripts, .nvmrc / pinned Node in CI to prevent environment drift in submissions. **PHASE 3 CODE-COMPLETE.**
2026-04-30 11:00  claude  --        Phase 4 driver scope expansion per user direction. v1 second-driver scope grows from OPC UA-only to {OPC UA, Modbus TCP/RTU, MQTT (incl. Sparkplug B), Beckhoff ADS}. Driver trait audit confirmed no extension needed (Capabilities flags + opaque TagAddress + optional TagNode browse already cover all five protocols). docs/feature-matrix.md and docs/roadmap.md updated to bump the four protocols to v1; website index.astro updated with the broader driver list and Phase 4 in-progress badge.
2026-04-30 11:00  claude  CODEX-W   Opened. Brief: crates/driver-opcua — OPC UA client driver via `opcua` crate. Address shape `ns=N;s=...`, MonitoredItem-based subscriptions, anonymous + username/password auth, browse via OPC UA Browse service. Sim harness, wiki entry, manual smoke. Cert auth out of scope for v1.
2026-04-30 11:00  claude  CODEX-X   Opened. Brief: crates/driver-modbus — Modbus TCP + RTU in one crate, two transports. Address shape `<unit>/<area>/<addr>:<count>:<type>` with explicit endianness suffixes. Master-only (slave mode post-1.0). Sim-modbus harness in TCP only (RTU coverage is unit-test).
2026-04-30 11:00  claude  CODEX-Y   Opened. Brief: crates/driver-mqtt — generic mode (raw topics with payload decoders) + Sparkplug B mode (NBIRTH alias-map + DDATA flow). rumqttc + prost. Vendored Sparkplug B protobuf. Sim-mqtt harness via rumqttd or docker mosquitto. Client cert auth + Sparkplug primary-host coordination post-1.0.
2026-04-30 11:00  claude  CODEX-Z   Opened. Brief: crates/driver-ads — Beckhoff TwinCAT ADS client. Address shape `<port>:<symbol>` (851 PLC, 350 NC, etc.). ADS device notifications, symbol upload, AMS net id resolution. ADS server mode + struct-as-single-tag post-1.0.
2026-04-28 23:55  codex   CODEX-U   Status -> in-progress. Building backend first: ScriptSource raw artifact storage, script event protocol, project_id propagation in scripting events, and gateway forwarding before Monaco UI.
2026-04-29 09:41  codex   CODEX-U   Status -> submitted. Added ScriptSource raw Python artifacts, script event streaming, gateway script-event forwarding/restart, Monaco designer editor/list/error pane, system.* completions, and tests across Rust+TS. Verified cargo workspace tests, clippy, fmt, pnpm typecheck/test, and designer Vite build (457.75 kB JS bundle).
2026-04-30 12:04  codex   CODEX-X   Status -> in-progress. Starting crates/driver-modbus plus examples/sim-modbus from the Phase 4 Modbus brief; first pass is driver-api/Rockwell reference shape, then address parsing, TCP sim harness, tests, wiki, and manual smoke docs.
2026-04-30 15:55  codex   CODEX-X   Status -> submitted. Added openwebhmi-driver-modbus, grouped polling reads, sim-modbus TCP slave, simulator-backed integration test, Modbus wiki page, feature-matrix/manual-smoke docs. Verified fmt, clippy -D warnings, package sim-tests, and full locked workspace tests.
2026-05-01 11:35  codex   CODEX-W   Status -> in-progress. Starting crates/driver-opcua plus examples/sim-opcua; first pass is resolving the opcua 0.13 client/server API, then driver, simulator, tests, wiki, and manual smoke docs.
2026-05-01 15:19  codex   CODEX-W   Status -> submitted. Added openwebhmi-driver-opcua, async-opcua-backed sim-opcua, simulator-backed read/write/browse/subscribe test, OPC UA wiki page, feature-matrix/manual-smoke docs. Verified cargo test -p openwebhmi-driver-opcua --features sim-tests.
2026-05-01 15:30  codex   CODEX-Y   Status -> in-progress. Starting crates/driver-mqtt plus examples/sim-mqtt after CODEX-W submission; first pass is generic MQTT cache/subscription flow, Sparkplug alias mapping, simulator, tests, wiki, and manual smoke docs.
2026-05-01 16:05  codex   CODEX-Y   Status -> submitted. Added openwebhmi-driver-mqtt, Sparkplug B alias-map parser, embedded rumqttd sim-mqtt, simulator-backed generic + Sparkplug integration test, MQTT wiki page, feature-matrix/manual-smoke docs.
2026-05-01 16:20  codex   CODEX-Z   Status -> in-progress. Starting crates/driver-ads after CODEX-Y submission; first pass is current ads crate API check, address/config/symbol support, simulator harness, tests, wiki, and manual smoke docs.
2026-05-01 16:40  codex   CODEX-Z   Status -> submitted. Added openwebhmi-driver-ads, sim-ads stub responder, ADS address/symbol/config support, simulator-backed read/write/browse/subscribe test, ADS wiki page, feature-matrix/manual-smoke docs. Noted real ADS wire compatibility remains pending.
2026-05-01 17:05  codex   CODEX-AA  Status -> in-progress. Starting MQTT v1 gap closure: TLS/WebSocket config, JSONPath payloads, binary big-endian decoding, broker readiness, and three-run sim-test flake gate.
2026-05-01 17:42  codex   CODEX-AA  Status -> submitted. Added rumqttc TLS/WSS/WS transport selection, JSONPath payload decoding, binary BE fixes, uncertain initial cache, active sim broker readiness, WebSocket sim coverage, docs/wiki smoke updates, and completed the 3-run MQTT sim-test gate plus workspace test/clippy.
2026-05-01 09:55  claude  CODEX-X   Reviewed. Status -> merged. First Phase 4 driver in. Spec-compliant on every callout: 11 datatypes with ABCD/CDAB endianness suffixes, FC limits enforced (125 reg / 2000 coils), exception code mapping (IllegalDataAddress→InvalidAddress; IllegalFunction/IllegalDataValue→UnsupportedType), single-vs-multiple FC selection, read-group coalescing on (unit_id, area, adjacent address), `=0.16.1` strict pin with upstream commit `b077ca3f...` recorded in wiki. ModbusClientLike trait abstraction lets unit tests run mock-driven. Sim-modbus is hand-rolled (no circular tokio-modbus-server dep) with a Notify hook for deterministic write tests. Wiki entry is the cleanest driver-wiki yet — Independent Verification Status table separating CI-validated from "pending hardware" RTU. v1.1 polish items added: no transport-level reconnect (codex flagged in wiki open questions); RTU lacks per-request timeout; gap-merge skipped (3 separate FC3 reads for addresses 100/200/300); single shared mutex serializes all I/O; no f64_le. These items share a class with the upcoming W/Y/Z reconnect concerns — likely a single bundled "driver hardening" v1.1 item. Three Phase 4 drivers remain: W (OPC UA), Y (MQTT), Z (ADS).
2026-05-01 11:35  codex   CODEX-W   Status -> in-progress. crates/driver-opcua + sim-opcua + wiki + smoke from CODEX-W brief.
2026-05-01 15:19  codex   CODEX-W   Status -> submitted. async-opcua 0.18 client + real OPC UA server sim + 4 NodeId forms + MonitoredItem subscriptions.
2026-05-01 15:55  codex   CODEX-Y   Status -> in-progress. crates/driver-mqtt with rumqttc + Sparkplug B protobuf + rumqttd in-process broker for sim.
2026-05-01 16:05  codex   CODEX-Y   Status -> submitted. Real MQTT wire protocol, real Sparkplug B protobuf decode, in-process rumqttd sim. TLS+WS deferred via UnsupportedType.
2026-05-01 16:20  codex   CODEX-Z   Status -> in-progress. crates/driver-ads on ads 0.4.4 (brief said 0.7, doesn't exist).
2026-05-01 16:40  codex   CODEX-Z   Status -> submitted. ads scaffold + JSON-line stub sim. Wiki notes it's not exercising ads crate against a real router.
2026-05-01 19:30  claude  CODEX-W   Reviewed. Status -> merged. Real wire protocol end-to-end: opcua::client::Session + MonitoredItem + Browse service; sim-opcua uses real opcua::server::ServerBuilder. async-opcua=0.18.0 strict pin; all 4 NodeId forms parsed (s/i/g/b); status code mapping (BadNodeIdUnknown→InvalidAddress); connect timeout + retry (250ms→8s). v1.1 polish: TLS verify defaults are `trust=true / verify=false` — needs a `tls_insecure` flag with explicit warn when set; NodeId Display/parse round-trip should be locked with a unit test. Three of four Phase 4 drivers in.
2026-05-01 19:45  claude  CODEX-Y   Reviewed. Status -> merged with caveats. Real rumqttc + real Sparkplug B 3.0.0 protobuf via prost + real rumqttd in-process broker. Acceptance criteria all green. **Three v1 scope gaps from the brief body didn't ship**: TLS + WebSocket transports (driver returns UnsupportedType); RawIntBe/RawFloatBe decode ASCII text not binary BE bytes (the test passes only because sim publishes "12.5" as text); json_path payload type missing entirely. Plus the integration test was flaky (failed once during review, passed retries). **Opening CODEX-AA** as focused follow-up to close the gaps before the feature-matrix can flip from "in development" to "simulator-validated". Same shape as CODEX-V was for T's brief error.
2026-05-01 20:00  claude  CODEX-Z   **Reviewed. Status -> rejected.** The submitted driver doesn't implement ADS wire protocol — opens a plain TcpStream and exchanges JSON-line frames (SimMessage/SimCommand) with the stub sim. The `ads` crate is imported only as a dead-code "version marker" function (worker.rs:252-255) so the dependency compiles. Real Beckhoff TwinCAT runtimes don't speak this dialect. **Brief error mine**: my brief pinned `ads = "0.7"` which doesn't exist on crates.io (latest is 0.4.4, verified). Brief amended in place with corrected `=0.4.4` pin + explicit "use ads crate's wire API end-to-end" requirement. Salvageable: address parser, symbol table data structure, manual smoke step, wiki scaffold (kept for reference). Rejected files (driver.rs JSON-line impl + sim-ads JSON responder + Cargo.toml/README/feature-matrix Z entries) removed from working tree. Status returned to `open` for rework.
2026-05-01 20:00  claude  CODEX-AA  Opened. Brief: close MQTT v1 scope gaps from CODEX-Y review — TLS via rumqttc::Transport::Tls + rustls; WebSocket via Transport::Ws; json_path PayloadType variant via jsonpath-rust; binary BE payload decoders (fix the ASCII-text bug); fix the integration test flake; add Uncertain quality state for "connected but no value seen yet". Acceptance: feature-matrix MQTT entries flip from "in development" to "simulator-validated".
2026-05-01 17:05  codex   CODEX-AA  Status -> in-progress. Focused MQTT v1 gap closure after CODEX-Z rejection handoff.
2026-05-01 17:42  codex   CODEX-AA  Status -> submitted. rumqttc TCP/TLS/WS/WSS transport selection, JsonPath PayloadType with serde round-trip, RawIntBe/RawFloatBe decode 8 raw BE bytes, configured tags seeded as Quality::Uncertain before first value, sim-mqtt waits for TCP/WS listeners, integration coverage for WS + binary BE + JSONPath + Sparkplug. TLS fixture remains external/manual. 3 consecutive driver-mqtt sim-tests runs green; workspace tests + clippy --all-features -D warnings green.
2026-05-01 21:00  claude  CODEX-AA  Reviewed. Status -> merged. All five brief items delivered: TLS via Transport::tls/tls_with_default_config; WebSocket via Transport::ws/wss; JsonPath via jsonpath-rust=1.0.4 with custom serde for both wire forms (`{"json_path":"$.path"}` and `{"json_path":{"path":"..."}}`); RawIntBe/RawFloatBe decode 8 BE bytes (with new Utf8Int/Utf8Float migration variants); seed_uncertain_cache populates configured tags with Quality::Uncertain on connect. Sim-mqtt now has separate WS listener + wait_for_listener readiness. 3-run flake gate verified. Feature-matrix MQTT entries flipped to simulator-validated. v1.1 polish: tls_insecure flag is a no-op (logs warn but doesn't bypass verification — either implement or remove); default trust source not documented (rumqttc 0.25's tls_with_default_config); no CI TLS test (intentional, external Mosquitto path documented); JsonPath returns only first match; flake fix is a 100ms sleep not a real readiness poll. **Phase 4 drivers: X + W + Y all simulator-validated. Only Z (ADS) remains.**
2026-05-01 21:30  claude  CODEX-AB  Opened. Brief: 8 new components — Gauge (SVG circular gauge with warn/alarm bands), ProgressBar (horizontal/vertical fill), Slider (write-back numeric with release/live commit), Dropdown (write-back selection with options array), ToggleSwitch (write-back boolean iOS-style pill), Button (click→tag.write a configured value, optional confirm prompt), MultiState (label/color from value match table), AlarmBanner (priority-band counts subscribed via onSubscribeAlarms). Bumps the component library from 8 → 16. Independent of driver work; can run in parallel with CODEX-Z. Brief includes shared concerns (designer-mode write suppression, bad-quality visuals, propsSchema), gotchas (objectList schema type may need to be invented for Dropdown options + MultiState states; Slider commit modes; Button's bound `target` prop), and acceptance gate of <1.5 MB component-library bundle.
2026-05-01 17:05  codex   CODEX-AB  Status -> in-progress. 8 new components, objectList schema, registry+exports, tests, smoke docs.
2026-05-01 17:27  codex   CODEX-AB  Status -> submitted. All 8 components shipped; objectList schema added to designer PropertyPanel; tests at 50; bundle 67,720 bytes (well under 1.5 MB). Z explicitly held for separate real-ADS pass.
2026-05-01 22:15  claude  CODEX-AB  Reviewed. Status -> merged with Claude-applied fix. 8 components shipped + objectList schema variant + designer-mode write suppression. **Real bug caught during review**: Slider/Dropdown/ToggleSwitch hardcoded write target as literal `"value"` instead of a configurable tag path — none could write to a real PLC tag. Fix applied during merge (small, mechanical): added `tagPath?: string` prop matching NumericInput's existing pattern; default `""` falls through to `"value"` so existing tests stayed green (50/50). v1.1 polish: read/write asymmetry (binding system gives read path but write-back needs separate config) is the architectural fix; Button.writeValue typed as TagValue | TagValue[]; AlarmBanner unbounded events array; Gauge bad-quality bands stay colored; reach to 25+ components needs one more batch. Codex's discipline holding off Z for a separate real-ADS pass is the right pattern.
2026-05-01 22:30  claude  CODEX-AC  Opened. Brief: 9 components to close the v1 25+ target — Tabs (multi-page layout, keyboard nav, bindable activeTab), Modal (operator dialog with focus trap + Confirm/Cancel + Escape key + click-outside, native <dialog>), DataGrid (sortable + paginated tabular view bound to JSON-array tag, no filtering for v1), BarChart and PieChart (categorical SVG charts; PieChart supports donut mode), Card/Spinner/Divider/Stepper (smaller filler primitives). Brief flags multi-child rendering (Tabs + Modal both need RenderProps.children — verify Container's path already plumbed before committing) and DataGrid bound-data JSON-string parsing (TagValue::Array protocol type is post-1.0). Risks/gotchas section also calls out Modal focus trap via native <dialog>, BarChart negative-value baseline, PieChart all-zero edge case. Acceptance flips feature-matrix "Component library (built-in)" row to "complete (25 components)" on merge. Independent of CODEX-Z; can run in parallel.
2026-05-01 17:14  codex   CODEX-AB  Status -> in-progress. Starting component-library batch 2; implementing TS/SVG components before returning to ADS because ADS needs a real wire API spike.
2026-05-01 17:27  codex   CODEX-AB  Status -> submitted. Added Gauge, ProgressBar, Slider, Dropdown, ToggleSwitch, Button, MultiState, and AlarmBanner; registered/exported all eight; added objectList schema support in the designer property panel; expanded component tests to 50 total; docs smoke steps appended. Bundle JS total 67,720 bytes. Verified component typecheck/test/build, pnpm -r typecheck/test, and locked Rust workspace tests.
