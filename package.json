{
  "name": "openwebhmi",
  "private": true,
  "version": "0.0.0",
  "description": "Open-source SCADA/HMI/MES platform — gateway-centric Rust runtime + Tauri designer + web HMI",
  "license": "MIT",
  "repository": "github:sergiogallegos/OpenWebHMI",
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r typecheck"
  }
}
